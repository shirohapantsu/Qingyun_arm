# 参数标定问题记录

## 问题清单

| Issue ID | 关联参数 | Severity | 问题 | 代码位置 | 状态 |
| -------- | ---- | -------- | -- | ---- | -- |
| CAL-ISSUE-001 | CAL-017、CAL-026、CAL-080~098 | P1 | 真机 profile 的夹爪端点仍是仿真 `500/3600`，越出官方 gripper 行程 `[1458,2980]`；任何 0/3/100% 命令会被最终 raw 行程闸拒绝 | `calibration/REAL_ARM/PC/profile.json`；`motor_control.py:_validate_and_encode` | OPEN |
| CAL-ISSUE-002 | CAL-015、CAL-016 | P2 | 电流/速度比例 `[1]/[3]` 来源于仿真或旧固件 2.54；实机固件已是 3.10，数值来源失效 | profile `motor.*_per_raw`；标定指南 4.3/A.2 | OPEN |
| CAL-ISSUE-003 | CAL-013、CAL-015、CAL-023、CAL-085~086 | P2 | vendor 编码表未登记 `Present_Current` 符号位，生产代码按无符号解码；尚无固件 3.10 手册或双向负载实测证据 | `qingyun/grabbing/motor_control.py:543` 附近；vendor `tables.py` | OPEN |
| CAL-ISSUE-004 | CAL-003~011 | P2 | 指南称已有真机实测，但其引用的 `dev_logs/真机标定问题记录.md` 不存在，`raw/`、`reports/` 也为空；现有证据只能追溯到指南文字，缺原始寄存器导出与命令记录 | `docs/真机参数测量与标定指南.md` 附录 A；`calibration/REAL_ARM/PC/` | OPEN |
| CAL-ISSUE-005 | CAL-012、CAL-020~107 | P2 | 真机 profile 仍有 397 个标量叶值与 simulation 完全相同；非 null 并不代表实测，存在把仿真副本误当真值的系统性风险 | 两份 `calibration/*/*/profile.json` | INVESTIGATING |
| CAL-ISSUE-006 | CAL-018~021 | P3 | 标定指南仍称 joints 硬件采集会丢 `sweep_joint_index` 等键，但当前未提交代码已用 `payload.update(row)` 修复且有端到端测试；文档与当前代码不一致 | 指南第 5 节/A.2/A.4/B；`scripts/calibrate.py:509-527`；`tests/test_calibration.py` | OPEN |
| CAL-ISSUE-007 | CAL-001、CAL-018、CAL-019、CAL-050 | P3 | 指南附录 A 仍用“现为 verified/sign=[1]/目录缺失”的现在时描述旧状态；当前 profile 已是 draft、sign/zero 已 null，raw/reports 目录也已存在 | 指南 A.2/A.4；当前真机包 | OPEN |
| CAL-ISSUE-008 | CAL-014 | P2 | `_fit_motor` 在有参考电流时仍将 `current_zero_raw` 写死为全 0，并非从零负载样本拟合；跑 fit 不能证明零偏已测 | `scripts/calibrate.py:_fit_motor` | OPEN |
| CAL-ISSUE-009 | CAL-015~016、CAL-022、CAL-049、CAL-051~079、CAL-084~098 | P2 | 多个 fitter 输出为硬编码、原值回抄或启发式派生，容易被误标为已标定 | `scripts/calibrate.py:_fit_*`；指南 1.4 B 类表 | OPEN |
| CAL-ISSUE-010 | CAL-008、CAL-009 | P2 | hardware capture 的 metadata 型号/固件来自 profile 回抄，不读取身份寄存器；虽已限制只有 hardware metadata 才能覆盖，但仍属自指证据 | `scripts/calibrate.py:_capture_hardware`、`_fit_motor` | OPEN |
| CAL-ISSUE-011 | CAL-014、CAL-015 | P2 | motor hardware capture 把 `torque_enabled` 恒写为 1，没有读回 Torque_Enable；零偏实验若实际关力矩，日志元数据会错误 | `scripts/calibrate.py:508` 附近 | OPEN |
| CAL-ISSUE-012 | CAL-029、CAL-040~041、CAL-099~107 | P2 | 多个字段运行期没有消费点；尤其视觉模块为空使 target bounds/视觉误差无法闭环验证，错值不会被控制代码拦截 | `qingyun/grabbing/vision.py`；指南 2.6 | UNVERIFIED |
| CAL-ISSUE-013 | CAL-003~107 | P2 | real 加载器只要求每阶段至少一条 physical check；hardware metadata 只证明来自真机总线，不能证明每个关键参数都有物理证据 | `configs/motion_params.py` 物理证据检查；`scripts/calibrate.py` 报告生成 | OPEN |
| CAL-ISSUE-014 | CAL-014~107 | P2 | validate/fit 含未配置的硬编码判据与系数（1 count、25 mA、10°、10 FK、9 桌面点、1000 tick、`×0.85+50`、`×2.2` 等）；部分无测量或需求依据 | `scripts/calibrate.py:1520-1705,1874-1956` | OPEN |
| CAL-ISSUE-015 | CAL-051~107 | P1 | timing/motion/grasp/pick_place 不支持 `capture --hardware`，要求运行期 JSONL 导出；但 `main.py`、`vision.py` 为空，生产模块也没有对应日志导出器，后四阶段当前无完整真机数据通路 | `scripts/calibrate.py:533-537`；`main.py`；`qingyun/grabbing/vision.py` | OPEN |
| CAL-ISSUE-016 | CAL-012、CAL-020~022 | P2 | 官方校准文件缺原始官方工具输出；`wrist_roll range=[0,4095]` 只是整圈范围，不能代表线缆应用限位，drive_mode/homing_offset 也未见逐字段核对证据 | `calibration/REAL_ARM/PC/motor_calibration.json`；指南 4.1/A.2 | OPEN |
| CAL-ISSUE-017 | CAL-071 | P2 | 当前 `ik.position_tol_m=0.007`，init 值为 0.006，按 `acceptance.max_position_error_m×0.6` 也应为 0.006；0.007 的来源未知 | 真机 profile；`scripts/calibrate.py:1608` | OPEN |
| CAL-ISSUE-018 | CAL-037~049、CAL-080~098 | P2 | 既定阶段顺序 workcell→grasp 与实际依赖冲突：workcell 扫描先需要 yaw offset/object envelope，正式 grasp 又在后；必须采用预值+grasp 后回归，否则工作域证据不成立 | 标定指南第 3/7/10 节；`scripts/calibrate.py:STAGES` | OPEN |
| CAL-ISSUE-019 | CAL-050、CAL-014~107 | P2 | 当前没有统一的环境基线记录（供电、基座固定方式、指垫、负载、温度/物体批次）；不同日期数据无法判断是否可合并 | 真机包 `raw/` 为空；JSONL metadata 设计 | OPEN |
| CAL-ISSUE-020 | CAL-106、CAL-105 | P2 | `acceptance.damage_observe_minutes` 不被 validate 用来判定每个样本观察时长是否达标，损伤率分母只看记录字段是否为真值 | 指南 2.6/11；`scripts/calibrate.py` pick_place validate | OPEN |
| CAL-ISSUE-021 | CAL-108、CAL-023、CAL-058~070、CAL-082~097 | P2 | 舵机 EEPROM 的 PID、死区、启动力和保护阈值会直接改变动态/安全结果，但当前标定包既不保存读回快照也不绑定其哈希；同一 profile 可能在不同 EEPROM 状态下表现不同 | vendor `libs/so_arm_core/motors/feetech/tables.py`；profile schema | OPEN |
| CAL-ISSUE-022 | CAL-003、CAL-109、CAL-024~049 | P2 | 现有 URDF 哈希只确认 vendor 文件身份，没有本机实际连杆长度、关节轴装配偏差或多姿态外部 FK 证据；把资源哈希当机械几何验证会漏掉装配误差 | URDF；指南 5.6/6.2 | UNVERIFIED |
| CAL-ISSUE-023 | CAL-040~042、CAL-110、CAL-103~107 | P1 | 相机到基座外参/尺度/畸变没有实现或版本化数据源，`vision.py` 为空；无法产生满足接口语义的真机目标中心与 yaw，也无法验证视觉误差界 | `qingyun/grabbing/vision.py`；技术文档 3.2 | OPEN |
| CAL-ISSUE-024 | CAL-022、CAL-028~029、CAL-064~070、CAL-111 | P2 | 关节齿隙和正反向回差未单独记录；单向零位拟合或静态 FK 精度可能掩盖方向相关误差 | joints/motion 测量数据；当前 `raw/` 为空 | OPEN |

## 处置原则

- P1 未关闭前不得驱动真机执行完整抓放；CAL-ISSUE-001 关闭前不得发送夹爪百分比动作。
- `OPEN/INVESTIGATING/UNVERIFIED` 问题不通过放宽验收阈值关闭，必须补原始证据、修改实现/文档或明确接受风险。
- 任何修复若改变 profile 或资源文件，按 `参数依赖关系.md` 第 6 节使下游结果失效。
- 本轮只建立审计文档并启动 CAL-014，未修改任何物理参数。
