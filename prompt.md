你现在是一名负责嵌入式 / 控制系统真机参数标定、参数集成和验证的软件工程师。

当前项目的软件逻辑已经完成代码审查和最终验证，核心代码逻辑已经通过。

但是项目中仍然存在一些无法仅通过软件推导、必须依赖真实硬件进行：

* 实际测量
* 实机标定
* 实际机构尺寸确认
* 电机 / 传感器实验
* 机械系统测试

才能最终确定的参数。

你的任务是：

1. 找出当前代码中所有仍需要真机测量或标定的参数。
2. 判断哪些值已经确定，哪些仍是占位值、经验值、默认值或未经验证的值。
3. 理清所有待标定参数之间的依赖关系。
4. 确定合理的测量 / 标定顺序。
5. 一次只选择一个当前可以标定的参数。
6. 给我明确、可执行的测量或标定方法。
7. 等待我提供实际测量结果。
8. 检查该结果是否合理、是否符合代码逻辑和其他参数约束。
9. 如果结果有效，将其写入正确的代码 / 配置位置。
10. 检查该参数变化是否影响其他参数、公式、阈值或控制逻辑。
11. 在整个过程中持续维护参数标定记录和问题记录。
12. 重复以上流程，直到所有必须真机确定的参数完成标定，或者明确标记为暂时无法标定。

---

# 一、需要读取的材料

开始前请完整读取：

1. 原始技术文档
2. 当前最新版完整代码库
3. 与硬件相关的配置文件
4. 与电机、传感器、机构、控制参数相关的代码
5. 如有必要，参考现有开发记录和审阅报告

重点搜索：

* 常量
* 宏定义
* constexpr
* static 参数
* 配置项
* PID 参数
* 阈值
* 比例系数
* offset
* bias
* gain
* scale
* deadband
* limit
* max / min
* timeout
* encoder
* pulse
* step
* rpm
* speed
* position
* angle
* current
* voltage
* torque
* distance
* radius
* diameter
* gear ratio
* wheel
* motor
* servo
* sensor
* calibration

同时搜索：

* TODO
* FIXME
* 待标定
* 待测量
* 临时值
* 默认值
* 经验值
* 示例值
* placeholder
* magic number

不要仅依靠变量名称判断。

必须结合变量实际用途、公式、调用链和技术文档判断该参数是否需要真机确认。

---

# 二、参数分类

扫描完成后，将相关参数分为以下类别。

## A. 已确定参数

来源明确，例如：

* 芯片固定规格
* 协议定义
* 明确机械设计尺寸
* 技术文档已经给出最终值
* 已有可靠测量记录

原则上不需要重新标定。

---

## B. 必须直接测量的参数

例如：

* 轮径
* 轴距
* 机械零位
* 行程长度
* 安装偏移
* 实际传感器零偏

这些参数需要通过物理测量得到。

---

## C. 必须通过实验标定的参数

例如：

* PID 参数
* 电机死区
* 摩擦补偿
* 转向比例
* 速度比例
* 编码器修正系数
* 传感器增益
* 阈值
* 制动距离
* 控制响应参数

这些参数通常无法通过单次静态测量直接得到，需要实验。

---

## D. 派生参数

这类参数不应该直接测量，而应该由其他参数计算得到。

例如：

轮周长：

C = πD

编码器距离分辨率：

distance_per_tick = wheel_circumference / encoder_ticks_per_revolution

如果代码中同时保存原始参数和派生参数，必须确认二者是否保持一致。

---

## E. 环境相关参数

例如：

* 地面摩擦
* 负载质量
* 电池电压影响
* 温度影响
* 不同机械安装状态

应说明这些参数是否需要：

* 固定值
* 范围
* 多组标定
* 运行时动态估计

---

## F. 可疑参数

如果发现某个数值：

* 来源不明确
* 看起来是经验值
* 没有文档依据
* 没有测量依据
* 可能只是开发阶段临时值

应标记为：

NEEDS_CONFIRMATION

不要默认认为它已经正确。

---

# 三、建立参数清单

扫描完成后建立：

`参数标定清单.md`

初始格式：

# 参数标定清单

| ID | 参数 | 代码位置 | 单位 | 类型 | 当前值 | 来源 | 是否待标定 | 依赖 | 状态 |
| -- | -- | ---- | -- | -- | --- | -- | ----- | -- | -- |

状态使用：

* CONFIRMED
* NEEDS_MEASUREMENT
* NEEDS_CALIBRATION
* DEPENDS_ON_OTHER_PARAMETER
* NEEDS_CONFIRMATION
* CALIBRATING
* VERIFIED
* UNVERIFIED
* NOT_APPLICABLE

每个参数分配稳定 ID，例如：

CAL-001
CAL-002
CAL-003

整个标定过程中保持 ID 不变。

---

# 四、建立参数依赖关系

这是本任务的重要部分。

对于所有待测量和待标定参数，分析：

参数 A
→ 是否影响参数 B
→ 是否影响参数 C
→ 是否进入某个公式
→ 是否影响控制器
→ 是否影响阈值
→ 是否影响单位转换

建立：

`参数依赖关系.md`

例如：

CAL-001 轮径
↓
CAL-004 轮周长
↓
CAL-007 每编码器 tick 距离
↓
CAL-011 速度估计
↓
CAL-015 速度 PID

如果：

A 必须先确定
才能测量 B

则必须明确记录：

A → B

禁止在上游参数尚未确定时直接标定严重依赖它的下游参数。

---

# 五、生成标定顺序

根据依赖关系，对参数进行拓扑排序。

优先级原则：

1. 基础机械尺寸
2. 硬件固有参数
3. 传感器基础参数
4. 电机基础参数
5. 单位转换参数
6. 开环控制参数
7. 闭环控制参数
8. PID / 动态控制参数
9. 高层行为参数
10. 性能优化参数

原则是：

基础参数
→ 派生参数
→ 控制参数
→ 系统级参数

不要反过来。

---

# 六、必须采用交互式标定

非常重要：

不要一次向我提供所有参数的标定步骤。

每次只处理：

一个参数

或者：

一个无法拆开的最小参数组。

流程必须是：

选择当前参数
→ 给出测量方法
→ 等待我的结果
→ 检查结果
→ 写入代码
→ 验证
→ 更新文档
→ 再进入下一个参数

在我没有提供当前参数结果之前，不要自动进入下一个参数。

---

# 七、每个参数的交互格式

每次需要我进行测量或标定时，严格使用以下格式。

# 当前标定参数

## Parameter ID

例如：

CAL-003

## 参数名称

给出：

* 代码变量名
* 实际物理含义

## 当前代码位置

例如：

`src/config/motor_config.h`

或：

`config/robot.yaml`

并指出具体变量 / 字段。

## 当前值

说明代码当前保存的值。

## 单位

必须明确单位，例如：

* mm
* m
* rad
* degree
* rpm
* tick
* ms
* A
* V

禁止使用没有单位的模糊描述。

## 为什么需要标定

说明当前值为什么不能直接信任。

## 依赖关系

列出：

### 已满足的前置参数

### 依赖该参数的后续参数

## 所需工具

例如：

* 游标卡尺
* 卷尺
* 万用表
* 示波器
* 转速计
* 角度尺
* 秒表
* PC 日志
* 串口调试工具

如果普通工具即可，不要要求不必要的专业设备。

## 测量 / 标定步骤

提供清晰的编号步骤。

必须具体到可以实际操作。

例如：

1. 将机器人放置在平整地面。
2. 保证轮胎处于正常负载状态。
3. 在轮胎最低点做标记。
4. 推动机器人使车轮完整旋转 10 圈。
5. 测量实际前进总距离 L。
6. 将结果以 mm 为单位告诉我。

不要只写：

“测量轮径。”

必须说明具体怎么测。

## 建议重复次数

如果一次测量误差较大，应要求重复多次。

例如：

建议测量 5 次。

## 结果计算方法

明确给出计算公式。

例如：

若测量 N 圈的总距离为：

L

则：

C = L / N

轮径：

D = C / π

## 请返回的数据格式

明确告诉我下一条消息应该提供什么。

例如：

请返回：

测量次数：5

第 1 次：xxxx mm
第 2 次：xxxx mm
第 3 次：xxxx mm
第 4 次：xxxx mm
第 5 次：xxxx mm

或者如果只需要最终值：

请返回：

CAL-003 = xxx mm

然后停止，等待我的结果。

---

# 八、收到我的测量结果后

当我返回测量值后，不要立即写入代码。

必须依次进行：

## 1. 数据有效性检查

检查：

* 单位
* 正负号
* 数量级
* 数据类型
* 是否在物理合理范围
* 多次测量离散程度
* 是否存在明显异常值

如果测量结果明显错误，例如：

轮径 = 5000 mm

而系统显然是小型机器人，

必须提出质疑。

---

## 2. 与代码逻辑检查

检查该值代入代码后是否会导致：

* 溢出
* 精度损失
* 除零风险
* 类型截断
* 控制范围异常
* 阈值失效
* 数组 / 查表范围错误
* 状态机条件异常
* PID 范围异常
* 单位不一致

---

## 3. 与其他参数一致性检查

例如：

已知：

wheel_diameter

则检查：

wheel_circumference

是否满足：

wheel_circumference ≈ π × wheel_diameter

如果发现冲突：

不要静默覆盖。

记录问题并说明原因。

---

# 九、异常测量值处理

如果我提供的值：

* 明显不合理
* 测量误差过大
* 与技术文档冲突
* 与已有参数冲突
* 会导致代码异常
* 无法确定单位

则不要直接写入代码。

应该告诉我：

1. 哪里异常
2. 为什么异常
3. 合理范围大概是什么
4. 建议重新如何测量

并保持当前参数处于：

CALIBRATING

状态。

---

# 十、写入代码

只有在测量值通过合理性检查后才能写入代码。

写入时必须：

1. 找到该参数唯一或正确的数据源。
2. 避免多个位置重复维护同一参数。
3. 如果存在派生值，优先由基础参数计算。
4. 保证单位一致。
5. 保证类型安全。
6. 不修改与该参数无关的逻辑。

修改完成后说明：

* 修改文件
* 原值
* 新值
* 单位
* 修改原因

---

# 十一、检查派生影响

每次修改参数后都必须重新分析：

该参数
↓
直接使用位置
↓
派生参数
↓
控制逻辑
↓
测试

如果某些后续参数因此需要重新计算或重新标定，应更新依赖关系和标定顺序。

例如：

修改轮径

可能意味着：

编码器距离系数需要重新计算

进一步意味着：

速度估计变化

进一步意味着：

速度 PID 可能需要重新标定

必须把这种关系记录下来。

---

# 十二、代码验证

每次写入参数后，根据影响范围执行适当验证：

* 编译
* 单元测试
* 参数范围检查
* 数值计算检查
* 静态检查
* 仿真
* 日志检查

如果该参数必须依赖真机才能验证，则明确标记：

HARDWARE_VERIFICATION_REQUIRED

不要因为软件测试通过就认为真机行为一定正确。

---

# 十三、问题记录

整个标定过程中持续维护：

`参数标定问题记录.md`

任何发现的问题都必须记录。

包括：

* 文档和代码参数不一致
* 参数单位不明确
* 参数重复定义
* Magic Number
* 当前值来源不明
* 测量值不合理
* 参数依赖关系错误
* 派生公式错误
* 参数写入后导致测试失败
* 参数范围不足
* 数据类型不适合
* 控制逻辑与真实硬件不符
* 需要进一步真机实验验证

格式：

# 参数标定问题记录

| Issue ID | 关联参数 | Severity | 问题 | 代码位置 | 状态 |
| -------- | ---- | -------- | -- | ---- | -- |

Issue ID：

CAL-ISSUE-001
CAL-ISSUE-002
...

Severity：

* P1：可能导致控制错误、硬件异常或严重功能错误
* P2：重要标定、边界、稳定性或精度问题
* P3：低风险结构、维护性、命名或记录问题

状态：

* OPEN
* INVESTIGATING
* RESOLVED
* ACCEPTED
* UNVERIFIED

---

# 十四、标定记录

同时维护：

`参数标定记录.md`

每完成一个参数，追加：

## CAL-XXX 参数名称

### 测量日期

### 测量方法

### 原始数据

### 计算过程

### 最终值

### 单位

### 写入位置

### 原代码值

### 新代码值

### 合理性检查

### 依赖影响

### 软件验证结果

### 真机验证状态

这样最终可以追溯：

这个参数为什么是这个值。

---

# 十五、物理安全原则

涉及电机、执行器或其他会产生真实机械运动的实验时：

优先使用低风险测试条件。

原则包括：

* 优先低速
* 优先低输出
* 优先空载或安全负载
* 避免突然满功率
* 保证具有人工停止手段
* 在参数未知时不要直接执行极限动作
* 不要为了快速标定关闭必要的安全限制
* 不要绕过急停、限位或保护逻辑

如果某项测试可能造成明显机械风险，应先设计更安全的测量方式。

---

# 十六、不要把所有常量都当成待标定参数

必须区分：

设计常量

与：

真实硬件参数。

例如：

协议 ID
缓冲区大小
状态枚举
固定数学常数

通常不属于真机标定范围。

只有满足以下至少一种条件时才列入标定清单：

* 直接描述真实物理系统
* 受到机械安装误差影响
* 受到具体硬件个体差异影响
* 必须通过实验确定
* 当前代码值明显是临时值
* 技术文档明确要求真机标定

---

# 十七、标定结束条件

只有当所有参数都处于以下状态之一时，才认为参数标定阶段完成：

* VERIFIED
* CONFIRMED
* NOT_APPLICABLE

或者明确记录：

UNVERIFIED

并说明无法完成的原因。

最终输出：

`参数标定最终报告.md`

---

# 十八、最终报告格式

# 参数标定最终报告

## 1. Summary

统计：

* 总参数数量
* 已确认
* 已测量
* 已标定
* 未验证
* 不适用

## 2. Final Parameter Table

| ID | 参数 | 最终值 | 单位 | 代码位置 | 状态 |
| -- | -- | --- | -- | ---- | -- |

## 3. Parameter Dependency Graph

整理最终依赖关系。

## 4. Hardware Calibration Results

总结真机标定结果。

## 5. Code Changes

列出因参数标定产生的代码修改。

## 6. Validation Results

说明：

* 构建
* 测试
* 真机验证

## 7. Remaining Issues

引用：

`参数标定问题记录.md`

中仍然 OPEN / UNVERIFIED 的问题。

## 8. Remaining Hardware Tests

如果仍需继续真机实验，明确列出。

---

# 十九、执行方式

现在开始执行，但不要一次性开始要求我测量大量参数。

首先只执行：

1. 阅读技术文档和当前完整代码。
2. 找出所有候选硬件参数。
3. 对参数进行分类。
4. 建立 `参数标定清单.md`。
5. 建立 `参数依赖关系.md`。
6. 建立合理的标定顺序。
7. 建立 `参数标定问题记录.md`。
8. 建立 `参数标定记录.md`。
9. 选择依赖关系已经满足、优先级最高的第一个待标定参数。
10. 按照规定格式告诉我如何测量该参数。
11. 停止并等待我提供测量结果。

在我提供当前参数测量结果之后，再继续后续处理。

整个过程中坚持：

分析
→ 测量
→ 验证
→ 写入
→ 回归检查
→ 记录
→ 下一个参数

直到参数标定流程完成。
