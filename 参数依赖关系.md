# 参数依赖关系

## 1. 总体依赖图

```text
已确认身份/协议
CAL-003~011
  ├─→ CAL-014 电流零偏
  ├─→ CAL-012 官方校准内容 ─→ CAL-017 夹爪端点
  ├─→ CAL-108 舵机 EEPROM 配置 ─→ 保护与动态参数
  ├─→ CAL-013 电流符号 ─┬→ CAL-015 电流比例 ─→ CAL-023/085/086
  │                       └→ 所有过流、接触和负载实验
  └─→ CAL-016 速度比例 ─────→ CAL-058~070、CAL-082/083/096/097

CAL-012 + CAL-018 sign + CAL-019 zero
  └─→ URDF 关节角
      ├─→ CAL-020/021/022 有效限位
      ├─→ CAL-024/025 工具变换
      ├─→ CAL-030~049 工作单元/路径几何
      └─→ CAL-058~079 运动与 IK

CAL-017 夹爪端点
  └─→ 夹爪百分比
      ├─→ CAL-025 TCP 平移表
      ├─→ CAL-026 gap 表 / CAL-027 angle 表
      └─→ CAL-080~098 抓取与夹爪参数

工具 + 关节 + 固定环境 + 物体/视觉误差
CAL-018~049
  └─→ CAL-051~057 时序
      └─→ CAL-058~079 运动/IK
          └─→ CAL-080~098 抓取
              └─→ CAL-103~107 系统级实物验收
```

CAL-050（最终供电、基座安装、指垫、负载、温度/物体批次）横向影响所有实验参数。环境变化不是自动重算项：必须按第 6 节的失效规则决定重测、按范围管理或建立多组 profile。

CAL-109（实际连杆/关节轴装配）是 CAL-003 文件模型与实物之间的桥梁；CAL-110（相机到基座外参）是视觉目标进入控制坐标系的桥梁；CAL-111（齿隙/回差）作为误差特性进入限位余量、工具误差和运动阈值。三者虽然没有独立的现有 profile 字段，也不能从依赖图中省略。

## 2. 基础换算与硬约束

### 2.1 位置与关节坐标

```text
CAL-011 resolution + CAL-012 [range_min, range_max]
  → mid = (range_min + range_max) / 2
  → count_to_deg = 360 / (resolution - 1)
  → q_bus_deg = (raw_position - mid) × count_to_deg

q_bus_deg + CAL-018 sign + CAL-019 zero_offset_deg
  → q_urdf_deg = sign × q_bus_deg + zero_offset_deg
```

任何 CAL-004/CAL-012/CAL-018/CAL-019 变化都会改变关节坐标真值，使 CAL-020~049、CAL-058~079 以及所有既有轨迹/验证样本失效。`homing_offset` 已写入设备 EEPROM，代码只登记而不再次加入公式。

### 2.2 夹爪百分比

```text
CAL-017 [closed_raw, open_raw]
  → gripper_pct = 100 × (raw - closed_raw) / (open_raw - closed_raw)
  → raw_command = closed_raw + pct/100 × (open_raw - closed_raw)

CAL-016 velocity scale + CAL-011 count_to_deg + CAL-017 endpoint span
  → gripper_speed_pct_s
```

因此 CAL-017 一旦变化，CAL-025、CAL-026、CAL-027 与 CAL-080~098 必须全部重测；不能只把旧表的横坐标线性换成新百分比。

### 2.3 电流与速度

```text
CAL-013 signed decoding + CAL-014 current_zero_raw + CAL-015 current_ma_per_raw
  → current_ma = (decoded_raw - current_zero_raw) × current_ma_per_raw
  → CAL-023 joints.hard_current_ma
  → CAL-085 contact_current_ma
  → CAL-086 gripper.hard_current_ma

CAL-016 velocity_deg_s_per_raw × decoded_velocity × CAL-018 sign
  → joint speed deg/s
  → 运动速度、到位速度、夹爪速度相关判据
```

### 2.4 有效关节限位

```text
CAL-003 URDF limits
CAL-020 measured_limits_deg
CAL-021 application_limits_deg
CAL-022 margin_deg
  → lower = max(urdf, measured, application lower) + margin
  → upper = min(urdf, measured, application upper) - margin
```

该交集直接约束 CAL-046、CAL-047、CAL-079 和全部 IK/轨迹。任何上游变化都要先重跑“空交集、示教位越限、raw 最终行程”检查，再允许低速运动。

## 3. 工具、工作域与碰撞依赖

### 3.1 工具与夹爪几何

```text
CAL-018/019 q_urdf + CAL-003 URDF
  → FK_E

CAL-109 实物几何一致性 + CAL-111 回差
  → FK 外部验证误差是否可由固定模型描述

CAL-024 rotation_e_tcp + CAL-025 translation_samples(gripper_pct)
  → T_E_TCP(g)
  → FK_TCP(q,g)
  → CAL-028/CAL-029 独立误差界

CAL-017 → gripper_pct
  ├─→ CAL-026 gap(g)
  └─→ CAL-027 active-finger angle(g) → 碰撞几何
```

CAL-025、CAL-026、CAL-027 的共同开度覆盖区间必须包含 CAL-080、CAL-081、CAL-084 对应的开度，以及 CAL-089+CAL-090；上游表不覆盖时只能补测，禁止外插。

### 3.2 抓取方向与 workcell 阶段循环

代码规定阶段顺序是 `tool → workcell → … → grasp`，但 workcell 的可抓取域扫描需要先知道 CAL-038，放置中心/持物包络还需要 CAL-039。这形成流程上的先验依赖：

```text
CAL-024/025/026/027
  → 预先直接测量/选择 CAL-037/038/039
  → CAL-030~049 workcell 扫描与示教
  → CAL-080~098 grasp 实验
  → 用 grasp 最终结果回归复核 CAL-042/048/049
```

解决方式不是打破 CLI 的报告顺序，而是在 workcell 前取得带 `PROVISIONAL` 说明的抓取偏移、方向和对象包络；grasp 实验确定最终值后，重新验证 workcell 与 place 路径。若最终 CAL-037/038/039 超出预值覆盖，workcell 阶段必须重采/重验。

### 3.3 误差与碰撞预算

```text
CAL-039 物体包络
+ CAL-040 视觉位置误差
+ CAL-041 yaw 外缘位移
+ CAL-028 工具位置误差
+ CAL-098 闭合/持物位移
  → 目标/持物膨胀包络

膨胀包络 + CAL-031 桌面平度 + CAL-035 净距
+ CAL-032 固定障碍 + CAL-033/034 连杆胶囊/豁免
+ CAL-036 轨迹子步
  → CAL-042 工作域、CAL-043~048 路径与放置点是否安全
```

CAL-029 当前没有运行期消费点；仍应由独立工具姿态验证产生，用于报告和未来误差预算。CAL-040/041 在视觉模块为空的当前版本无法完成，故所有依赖它们的系统级结论只能是 UNVERIFIED。

```text
CAL-030 基座/桌面基准 + CAL-109 实物机构
  → CAL-110 相机到基座外参
  → CAL-040/CAL-041 视觉误差界
  → CAL-042 目标域与 CAL-103~107 系统验收
```

上式最后一项指 CAL-042 与 CAL-103~107；相机外参不属于当前运动 profile，但必须由视觉侧保存唯一数据源和版本/哈希，避免把固定偏差错误塞进 CAL-037/038。

### 3.4 抓取与放置位姿

```text
p_grasp = target.position + Rz(target.yaw) × CAL-037
yaw_grasp = target.yaw + CAL-038
T_B_TCP_grasp = top_down_pose(p_grasp, yaw_grasp)

CAL-048 PlacePose + FK_TCP(q_close,g_close)
  → T_TCP_O
  → T_B_TCP_place
```

CAL-048 记录的是释放时物体中心而非 TCP。改变 CAL-025、CAL-037、CAL-038、CAL-039、CAL-098 或指垫后，旧 place 验证全部失效。

## 4. 时序、运动、IK 与夹爪派生关系

### 4.1 时序

当前 fitter 的实现关系为：

```text
CAL-051 read_timeout  = max(P99(read)×1.5, max(read))
CAL-052 write_timeout = max(P99(write)×1.5, max(write))
CAL-053 feedback_span = max(P999(span), max(span))×1.2
CAL-054 feedback_age  = CAL-053 + max(read) + 0.020 s
CAL-055 tick_lateness = min(0.020 s, 0.6/FPS)
CAL-056 stop_poll     = min(0.3/FPS, 0.010 s)
CAL-057 planning_poll = min(0.05 s, 2×observed stop latency)
```

这些系数是启发式，不是物理定律。≥1000 tick 是最小数据要求；在最终 CPU 负载、USB 拓扑和供电下必须验证。CAL-010=1 代表写操作无设备 ACK，CAL-052 只证明一帧提交给串口，动作落盘要靠后续反馈判断。

### 4.2 运动约束

```text
CAL-058 max joint velocity / FPS → CAL-060 command step 上界
CAL-058 + 实测二阶差分 → CAL-059 acceleration
CAL-064 soft following error ×2.2 → CAL-065 hard following error
静止与到位误差 → CAL-067/068/069/070
CAL-024~049 的 FK 路径 → CAL-061/062 TCP 速度与加速度
```

CAL-023、CAL-065 是机械保护边界；必须基于器件限制、负载和可接受几何偏差独立确认，不能仅接受 fitter 乘数。

### 4.3 IK

```text
CAL-099×0.6 → CAL-071 position_tol（当前代码拟合关系）
CAL-100×0.5 → CAL-072 tilt_tol
CAL-101×0.5 → CAL-073 yaw_tol

CAL-071~073 + CAL-074 weights + CAL-075~078 停止判据 + CAL-079 seeds
  → CAL-042 覆盖域内的收敛率、残差与最坏求解时间
  → CAL-057 与 CAL-102 停止延迟预算
```

当前 CAL-071=`0.007 m` 与按 CAL-099 的 fitter 公式所得 `0.006 m` 不一致，未查到来源；在确认前不得把它视为已派生。

### 4.4 夹爪接触

```text
CAL-026 + CAL-039 + CAL-038 → CAL-080 preopen
CAL-026 + 首次接触试验 → CAL-084 contact gap
CAL-014/015 + 接触前后电流 → CAL-085 contact current
CAL-085×2（当前 fitter） → CAL-086 hard current
CAL-002 + CAL-083 + CAL-087 + CAL-088
  → 检测延迟和额外闭合/压缩量
空闭合循环 → CAL-089/090/091/092/096/097
持物/松爪试验 → CAL-093/094/095/098
```

如果检测延迟对应的额外压缩量超限，应优先降低 CAL-083、缩短合理的窗口/持续时间或改进指垫；不得靠提高 CAL-085/086 掩盖。

## 5. 拓扑排序后的标定顺序

已完成的 CAL-002~011 不重复测，除非硬件、EEPROM、USB 适配器或固件发生变化。后续每次仍只处理一个参数/最小不可拆组。

1. **当前可独立进行：CAL-014 电流原始零偏**（只读、力矩关闭、无机械运动）。
2. CAL-012 官方校准文件逐项一致性与 CAL-108 舵机 EEPROM 只读快照；若文件变化，立即重算 CAL-004。
3. CAL-013 电流符号编码；随后 CAL-015 电流比例、CAL-016 速度比例。
4. CAL-017 夹爪安全闭合/张开 raw 端点。
5. CAL-018、CAL-019、CAL-020、CAL-021、CAL-022、CAL-023，并量化 CAL-111 正反向回差。
6. 抽检 CAL-109；再做 CAL-024、CAL-025、CAL-026、CAL-027、CAL-028、CAL-029。
7. 固定 CAL-050 的正式实验环境；在调参前确认 CAL-099~107。
8. 预先测 CAL-039，选 CAL-037、CAL-038 的保守预值。
9. CAL-030、CAL-031、CAL-032、CAL-033、CAL-034、CAL-035、CAL-036。
10. CAL-110、CAL-040、CAL-041（视觉实现并可测后）；随后 CAL-042~049。
11. CAL-051~057（最终平台 ≥1000 tick 与停止注入）。
12. CAL-058~070，再做 CAL-071~079 的 IK 扫描和时间预算回归。
13. CAL-080~098；得到最终 CAL-037/038/039 后回归 CAL-042/048/049。
14. 按 CAL-103~107 做独立 pick/place 统计，八阶段物理证据均通过后才 promote。

如果 CAL-040/041 因视觉模块未实现而无法进行，则 CAL-042、CAL-048、CAL-049 与系统级抓放验收均标 `UNVERIFIED`，不得用 mock 结果替代。

## 6. 变更后的失效与重测规则

| 发生变化 | 至少失效/重测 |
| -- | -- |
| 电机/EEPROM homing/官方校准文件 | CAL-004、CAL-012、CAL-017~049、CAL-058~098、CAL-108~111，所有阶段报告哈希 |
| 固件、型号或 EEPROM 控制/保护参数 | CAL-008~016、CAL-023、CAL-051~070、CAL-082~097、CAL-108、CAL-111 |
| 关节 sign/zero | CAL-020~049、CAL-058~079，既有示教位和轨迹 |
| 夹爪 raw 端点 | CAL-025~027、CAL-080~098 |
| 工具/指垫/夹持深度 | CAL-024~029、CAL-033~049、CAL-080~107 |
| 基座、桌面、障碍或相机安装 | CAL-030~049、CAL-071~079、CAL-099~107 |
| 连杆/关节装配或模型几何 | CAL-003、CAL-020~049、CAL-071~079、CAL-109、CAL-111 |
| 负载、供电、CPU/USB 拓扑 | CAL-015/016/023、CAL-051~070、CAL-082~107 |
| object envelope/批次 | CAL-039、CAL-042~049、CAL-080~107 |
| acceptance 阈值 | 相关 IK/时序/抓取参数与全部 validation 报告 |
