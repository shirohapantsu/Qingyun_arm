# upper 功能开发执行任务

你是本项目的主 agent，具备创建、调度和接收 subagent 成果的能力。请依据当前仓库的实施文档及本文的分工范围，组织 subagent 完成 upper 草莓 demo 非视觉部分的代码新增、修改、测试和集成，视觉部分只保留接口。直接执行开发工作，不要只输出方案或任务清单。

**视觉实现由用户的队友负责。本任务不得实现 `qingyun/grabbing/vision.py` 的视觉逻辑，也不得把同类逻辑转移到其他文件中实现。保留公共契约、调用方和独立测试替身，让非视觉部分可以先完成开发。**

**本任务以用户不在线监视的方式执行。编码过程中遇到问题，由 agent 自行分析、选择方案、实现并验证，不向用户提问或等待回复。所有自行处理的不确定事项单独记录；用户会在全部编码完成后开启新的代码审查任务。主 agent 在本任务中的逐项验收仍须正常执行。**

## 1. 执行目标与依据

先阅读项目适用的 `AGENTS.md`、[实施文档总览](docs/实施文档/README.md)及以下六份实施文档，再结合实际代码确认起点：

1. [P0 公共契约与迁移](docs/实施文档/P0_公共契约与迁移技术文档.md)
2. [P1 任务编排与运行生命周期](docs/实施文档/P1_任务编排与运行生命周期技术文档.md)
3. [P2 语音与云模型接入](docs/实施文档/P2_语音与云模型接入技术文档.md)
4. [P3 视觉检测与目标选择](docs/实施文档/P3_视觉检测与目标选择技术文档.md)
5. [P4 标定工具与参数发布](docs/实施文档/P4_标定工具与参数发布技术文档.md)
6. [P5 集成测试与部署验收](docs/实施文档/P5_集成测试与部署验收技术文档.md)

本任务规定协作方式；实施文档规定功能、接口、状态机、配置、异常、测试及验收要求。用户在当前会话中的明确指示优先。历史审查记录用于理解背景，其中的旧方案、建议、命令或“已完成”描述不能替代现行规格和实际检查。已有代码用于判断迁移工作量，不能以旧行为否定已确认的新规格。

本文对视觉开发范围的排除优先于实施文档中要求本 agent 实现完整 P3、视觉标定或原生视觉回放的安排。仍需阅读相关规格以保持接口一致，但不得将这些实现任务分配给 subagent。

实施文档中“遇到不确定事项向用户逐个提问”的旧工作方式，在本次编码任务中由本文第 7 节的自主处理与记录规则替代。不得因等待用户确认普通工程问题而停止推进。

本期仅实现草莓 demo。遵守总览中的 D01–D20 及其修订关系，不扩大为蓝莓功能，不重新询问已经确认的决定。尤其注意：

- 生产启动要求六轴全部卸力，否则拒绝启动且不追加舵机写入。
- 草莓实物长、宽、高由用户保证处于运动包络内，不额外引入逐个量具登记要求。
- 视觉方案仍采用 OBB 区域深度点、桌面剔除及基座系几何估计，不新增分割模型；该要求仅作为队友交接背景，本 agent 不实现算法。
- ASR 使用 `qwen-audio-3.1-asr-flash` 和北京 DashScope；LLM 使用 `deepseek-flash` 和 DeepSeek 官方 API。不得自行替换模型来掩盖接入失败。

完成目标是交付本任务范围内符合规格的代码及可复现的软件验收证据。视觉实现及其完整联调由队友交付后再验收；真实标定、账号接入验证和真机验收另行记录实际完成情况。本次通过不代表完整视觉链路或真机通过。

### 视觉分工与接口边界

| 本任务必须完成 | 留给队友，不在本任务实现 |
|---|---|
| P0 的六字段 `VisionInterface`、`VisionThresholds` 及消费方迁移 | 相机 SDK/UVC 接入、RGB-D 采集同步、D2C、YOLO-OBB 推理 |
| `vision.py` 的异常类、公开函数签名、类型说明和明确失败的接口桩 | 深度点处理、桌面剔除、坐标变换和几何测量、候选过滤/排序/ignore 选取算法 |
| main/plans 的视觉调用、阈值注入、结果消费、分级、异常处理和生命周期衔接 | 视觉配置解析、权重加载、视觉扫描审计、原始帧回放和视觉专用测试资产 |
| 独立 FakeVision 及调用方契约测试，使用预设目标对象或异常驱动业务流程 | 相机内外参标定、视觉精度/足迹验证、图像采集及其他视觉算法工具 |
| 非视觉的运动标定、参数加载、记录完整性检查与发布门禁；消费既定数值/报告格式 | 从图像提取标定数据或重新计算视觉测量值的逻辑 |

具体要求：

- 按 P1/P3 保留 `NoTarget`、`VisionHardError`、`configure(thresholds, config_path)`、`init()`、`get_target(ignore=0)` 的准确签名、类型及语义说明。尚未实现的生产接口调用应抛 `VisionHardError` 并明确说明视觉待队友实现，不能返回固定目标、空成功或用 `NoTarget` 掩盖模块缺失。
- 桩可被正常导入，不加载相机 SDK、检测运行时或权重，不连接设备。若已有队友的视觉实现，保留其代码，不覆盖为桩、不自行改写；接口差异记录在交接文件中，由调用方通过既定契约对接。
- FakeVision 放在测试支持代码中，通过 P1 约定的替换方式供测试和独立 mock 入口使用；复用真实公共类型和异常类，记录调用参数，按预设事件返回目标或抛异常。不得在替身中实现图像处理、候选筛选、几何计算或原生 replay，也不得让生产入口自动回退到替身。
- 非视觉代码继续实现分级、槽位分配、失败计数和恢复/退出等既定业务行为。仅引用视觉类型或调用接口不属于视觉算法实现，不能因此把这些业务也留空。
- 混合工具仅实现非视觉流程及视觉接口调用，离线用 FakeVision 验证。K 的数值拟合可消费已提供的尺寸与人工标签；从图像采集尺寸、标签识别及视觉标定留给队友。缺少真实视觉实现或证据时，保持明确失败和原有发布门禁。
- 不为完成本次离线验收安装相机/YOLO 专用依赖，不额外创建视觉实现任务。视觉文件和公共接口的归属在任务台账中标明，避免与队友同时修改。

由主 agent 编写 `dev_logs/upper开发/视觉接口交接.md`，列出公共类型与字段单位、函数签名、异常语义、调用顺序、配置路径约定、日志/资源生命周期职责、调用方位置、FakeVision 用法、留给队友的文件与验收条目。保留原规格要求，注明本次只完成接口；不另行发明一套视觉契约。

## 2. 主 agent 与 subagent 的职责

**严格串行调度：整个任务树同时最多有一个正在执行任务的 subagent。** 不并行分配编码、测试、审查或调研 agent；禁止 subagent 再创建子 agent。上一任务完成交付并经主 agent 验收通过后，才分配下一项任务。

主 agent 负责：

- 阅读规格与代码，拆分任务、明确依赖、确定修改范围和验收条件。
- 按顺序向一个 subagent 下发具体编码任务，保留其标识与当前任务状态。
- 审阅实际代码差异、开发日志、测试结果和与既有模块的兼容性，独立作出验收结论。
- 对不合格成果提出具体返工要求；返工仍只交给一个 subagent，验收前不启动依赖它的后续任务。
- 维护任务台账和自主决策记录，处理跨阶段接口问题及不确定事项，向用户报告进展及最终结果；进展报告不要求用户回复。

subagent 负责：

- 阅读分配任务对应的实施章节与相关代码，在约定范围内完成实现、必要测试及配套文档更新。
- 自查差异、执行验收命令、修复发现的问题，并在完成后编写开发日志。
- 交付代码、测试证据、日志路径和遗留事项；不得自行宣布阶段或整个项目验收通过。
- 若需要越过修改边界或改变公共契约，先向主 agent 说明依据，不擅自扩展任务。

主 agent 可以在 subagent 工作期间阅读代码和准备验收，但不要同时修改该任务涉及的文件。功能编码及返工交给 subagent，主 agent 负责调度与验收。若工具不支持 subagent，明确报告该限制，不伪造分工，也不默默改为单 agent 执行。

## 3. 启动检查与顺序安排

开始时检查工作区状态、实际入口、公共接口、已有测试及可用 Python 环境。记录已有未提交改动，保留用户和之前任务的成果；不得执行清理、重置或覆盖来获得“干净基线”。已有变更与本任务重叠时，先读懂其用途，再做必要的增量修改。

创建并持续维护 `dev_logs/upper开发/任务台账.md`，至少记录：任务 ID、所属阶段、目标、依赖、修改范围、对应规格及必测条目、subagent 标识、状态、开发日志路径、主 agent 验收结论和剩余阻塞。根据代码实况安排工作，不把文档中的历史进度当作当前实现状态，也不重复重写已经验收的功能。

同时创建 `dev_logs/upper开发/自主决策与待审查问题.md`，由主 agent 统一维护。为每项不确定事项分配稳定编号（如 `DEC-001`），关联任务、开发日志、最终代码和验证证据；subagent 将问题与处理建议交给主 agent 归档，避免仅散落在会话或各自日志中。

默认按以下顺序串行推进，每个阶段可拆成多个可独立审阅的小任务：

| 阶段 | 本阶段交付重点 |
|---|---|
| P0 | 公共接口及消费方迁移、阈值类型、prompt 收窄和回归 |
| P1 | 配置与预检、方案注册、生命周期与恢复/退出、日志、主入口、共享会话及离线装配基础 |
| P2 | 音频输入、ASR/LLM 适配、超时与响应校验、离线故障测试 |
| P3 接口交接 | 仅核对接口桩、公共契约与调用方，编写视觉交接文档；P3 实现和算法测试交队友 |
| P4 非视觉软件部分 | 运动标定与采集流程、draft 参数加载、实测记录校验和发布门禁；视觉工具只保留必要接口与交接说明 |
| P5 非视觉离线部分 | 使用 FakeVision 复用生产业务逻辑的 mock 链路、业务集成场景、部署检查与验收报告；完整视觉回放待队友交付 |

非视觉部分的依赖关系以实施文档为准。本次 P5 离线业务集成在 P0/P1 契约就绪后即可使用 FakeVision 推进，不等待 P3 实现，不切换到原生视觉 replay。P4 视觉工具不纳入调度。始终只有一个 subagent 执行任务；不要等待真实 K、places 或其他实测值后才实现 mock，也不要把模拟数据当成生产参数。

遇到真正阻塞时，先记录该任务为阻塞状态并结束或停止对应 subagent，再安排不依赖该阻塞的任务。未通过验收的前置任务不得标为完成。全部可执行的软件任务完成前，不因完成一个阶段而自行结束整个工作。

“存在待后续审查的自主决定”本身不构成阻塞：只要主 agent 已选定可行方案，代码和必要软件测试已通过，就继续推进依赖任务。资源确实无法取得或操作权限不足时，按第 7 节隔离受阻部分，不以记录问题代替完成可执行的编码工作。

## 4. 每次下发任务的格式

不要只告诉 subagent“实现 P1”或“参考文档自行处理”。每次至少明确以下内容：

```text
你是编码 subagent，不得再创建 subagent。

任务 ID：<例如 P1-02>
目标：<本次必须交付的具体行为>
规格依据：<实施文档路径、章节、相关决策与必测条目>
前置成果：<已验收接口、配置、文件及必须保留的已有改动>
修改范围：<允许新增/修改的文件或模块，以及需要先报告的跨界修改>
视觉边界：视觉由队友实现；本任务只可涉及既定接口、非视觉调用方和独立 FakeVision，不得实现视觉算法或工具。
实现要求：<输入输出、异常与边界、资源释放、兼容或迁移要求>
验证要求：<必须覆盖的场景、预期行为和可执行命令；环境命令先核实>
开发日志：dev_logs/upper开发/<任务 ID>_开发日志.md
不确定事项：自行查证、提出可行方案并完成范围内实现；跨任务或公共契约问题交主 agent 决定，不向用户提问。
完成条件：实现、自查和验证完成，日志写入后再交付。

禁止通过削弱断言、跳过必要测试、伪造实测值或绕过生产门禁使结果通过。
发生规格冲突或阻塞时，向主 agent 提供具体证据、备选方案及建议，由主 agent 决定并记录后继续。
交付时列出改动文件、验证结果、日志路径、自主处理的问题和未完成事项。
```

任务大小以能够明确验收为准。涉及公共接口时，将本任务范围内的生产消费方、测试替身、配置和相关文档迁移纳入明确任务，避免只改定义而漏改调用方。视觉接口桩是本次明确允许保留的交付物，由队友后续替换；其他阶段性模块桩仍必须按规格替换，不能将桩、固定成功返回或空函数作为非视觉功能的最终实现。

## 5. subagent 开发日志要求

每项任务完成编码和自测后、向主 agent 交付前，必须写入开发日志。使用中文，基于实际执行记录，至少包含：

1. **任务与依据**：任务 ID、目标、对应实施章节及验收条目。
2. **实现说明**：修改文件及关键符号，行为变化、关键选择及其规格依据。
3. **验证记录**：工作目录、解释器或环境、实际命令、退出码、通过/失败/跳过情况和必要输出摘要。
4. **覆盖关系**：本任务负责的必测条目对应哪些测试，哪些尚未验证及原因。
5. **问题与限制**：仍有的失败、环境缺失、待实测项、跨阶段影响；无则明确写无。
6. **交接事项**：供主 agent 重点审阅的位置、下一任务可用的接口或产物。
7. **自主决策**：本次遇到的规格缺口、冲突或假设，实际采用的方案、验证结果及关联的 `DEC` 编号；已经解决的问题也必须保留记录。

日志不得记录密钥，不得将未执行的命令写成通过，不得将测试未收集、缺依赖或全部跳过描述为测试成功。返工时追加修正与重测记录，保留问题及其解决过程。

## 6. 主 agent 验收与返工

收到交付后，按以下顺序验收：

1. 阅读开发日志并检查实际差异，确认改动范围、实现与规格一致，且保留原有工作。
2. 检查公共契约、状态分支、错误传播、资源关闭和配置路径是否完整，确认测试验证实际行为而非仅复述实现。
3. 独立运行本任务关键测试或复现命令，审查 subagent 的其余测试证据。阶段交接前执行本次范围内的完整验收与必要回归，不以 subagent 的口头“通过”代替证据。
4. 将必测条目映射到真实测试及结果。视觉算法、视觉标定及完整原生 replay 条目标为“队友负责／本次未验收”，保留编号与原因；混合 E2E 条目说明本次由 FakeVision 覆盖的业务分支和仍待真实视觉验证的部分，不把替身结果标为原条目全部通过。其余强制条目不得遗漏。无需因无关小改动反复运行全套测试，但新改动、失败或未解决疑点必须补验。
5. 在任务台账写明结论、依据、执行命令、遗留事项和后续可开始的任务。

验收状态使用：

- `通过（软件/离线）`：任务范围内实现与必须执行的软件检查已通过，真实设备事项另列；如含自主决定，关联 `DEC` 编号，注明仍需后续独立审查。
- `退回修改`：列出缺陷、代码位置、违反的规格和重测要求，交给 subagent 返工后重新验收。
- `阻塞/未验收`：缺少无法自行补齐的必要环境、输入或权限，无法形成通过证据；注明已尝试的解决办法和解除条件。
- `队友负责／本次未验收`：视觉实现及依赖真实视觉的验收，列明交接项；这不阻塞本次非视觉代码和 FakeVision 业务链路的验收。

发现实现缺陷必须修正，不能把仍可修复的问题仅写入日志便继续依赖任务。环境不满足时先尝试合理修复；仍无法执行的必测检查保持未验收，不宣称代码正确或测试通过。

## 7. 自主处理问题与单独记录

遇到规格缺口、文档冲突、实现困难、依赖不兼容或测试失败时，先检查相关文档、调用关系和可复现证据，必要时查阅官方资料或进行最小实验。主 agent 根据证据选择能够完成目标、改动范围合理且便于后续修正的方案，交给 subagent 实现并验证，不等待用户决定。

明确的用户决定与安全边界仍是约束，不能以“自主处理”为由取消。对尚未定义的行为，可以采用明确标注的工程假设；对已确认要求确实无法实现的部分，完成可实现的接口、错误处理与离线验证，清楚记录仍未满足的要求，不把假设写成用户确认。需要调整公共接口时，由主 agent 统一安排所有消费方、配置和测试同步修改，记录与原规格的差异及原因。

自主决策不得扩大为接管队友的视觉开发，也不得擅自变更已交接的视觉契约。视觉实现缺失是明确的分工，不是需要 agent 补齐的代码缺陷；遇到接口冲突，优先保留既定契约并隔离差异，记录到自主决策和视觉交接文件。

`dev_logs/upper开发/自主决策与待审查问题.md` 中，每项至少记录：

| 字段 | 记录要求 |
|---|---|
| 编号与关联 | `DEC` 编号、任务 ID、实施文档章节及开发日志路径 |
| 问题与证据 | 原要求、具体缺口或冲突、复现条件和关键证据 |
| 方案与依据 | 考虑过的可行选项、最终选择及简明理由，不记录冗长思考过程 |
| 实现与影响 | 文件路径及关键符号、影响的接口和行为、与原规格的差异 |
| 验证结果 | 实际测试命令、结果、尚不能验证的部分 |
| 后续审查 | 需要新审查任务重点判断的假设、剩余风险、替换或回退方案 |
| 状态 | 已实现并验证／已实现待验证／外部条件阻塞；独立审查结论初始为待审查 |

不确定事项即使已被 agent 解决，也必须保留在该文件中。普通编码步骤写开发日志即可，不要用大量无关记录淹没真正需要审查的决定。已知且可以修复的缺陷必须在本次修复，不能以“用户稍后审查”为理由留下。

通过实现、测试和清晰报错处理缺失资源，不虚构设备、模型权重、账号权限或测量结果。生产 profile 的 `verified` 发布必须满足文档要求的真实证据；禁止翻转状态、复制 mock 参数或放宽加载校验来绕过发布条件。

默认使用离线替身、固定输入和本地故障注入完成编码验证。真实舵机动作、标定采集或付费云调用仅在所需资源和已有明确授权均具备时执行；缺少条件时记录为待后续执行，不在本次任务中发起等待用户答复的确认，也不将用户暂时不在线视为授权。完成相应适配代码、校验逻辑和离线测试，继续推进其余软件实现。

不得擅自提交、推送或发布代码。不要提交密钥、真实凭据或本地私有配置。仅同步本次实现必要的文档，不通过改写验收规格来迁就错误实现。

## 8. 持续推进与最终交付

每次验收后更新台账，再顺序分配下一任务。会话恢复或上下文压缩后，先检查台账、实际代码、最近开发日志及现有 subagent 状态，避免重复派工或同时启动第二个 subagent。持续向用户简短报告已验收成果、下一步和真实阻塞。

本次软件部分完成时，必须具备：符合 P0–P5 本次非视觉范围要求的代码、视觉接口桩与交接文档、逐项验收记录、subagent 开发日志、自主决策与待审查问题记录、可运行的 FakeVision 离线业务链路及结果、准确的部署说明，以及独立列出的视觉联调/云端/标定/真机待验事项。不要把“非视觉代码完成”“完整软件已验收”“真机已验收”混为一谈。

完成编码后，由主 agent 编写 `dev_logs/upper开发/代码审查交接.md`，供用户开启的新任务直接使用。该文件应包含：开发前基线和本次改动范围、P0–P5 分项完成情况、视觉排除范围及视觉接口交接链接、运行与复测命令、关键验证结果、全部 `DEC` 条目的索引及审查优先级、偏离原规格的位置、未验收事项，以及任务台账与各开发日志的链接。原有未提交改动与本次新增修改应尽可能区分；不为记录基线而擅自创建提交。

最终答复应说明实际完成范围、关键验证结果，链接任务台账、自主决策记录和代码审查交接文件，并列出真实未完成事项及解除条件。后续独立代码审查由用户在新任务中发起，本次不等待审查回复。若仍存在阻塞，明确未完成范围，不宣称全部完成。

现在开始：检查仓库与实施文档，建立任务台账，确定第一个尚未完成且前置条件满足的编码任务，然后只向一个 subagent 下发该任务。
