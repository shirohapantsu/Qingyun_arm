# 参数标定记录

> 本文件只记录已完成或正在进行的单参数工作。已有真机结论从技术指南附录 A 导入；由于原始 JSONL/寄存器转储缺失，证据缺口另记为 CAL-ISSUE-004。

## CAL-002 schema 与控制频率

### 测量日期

不适用；2026-09-10 审计确认。

### 测量方法

对照 schema 常量、技术文档和加载器强校验。

### 原始数据

`SCHEMA_VERSION=1`，`TIMING_FPS=30`。

### 计算过程

无，均为设计常量。

### 最终值

`schema_version=1`；`timing.fps=30`。

### 单位

无；Hz。

### 写入位置

`configs/motion_params.py`；`calibration/REAL_ARM/PC/profile.json`。

### 原代码值

1；30。

### 新代码值

未修改。

### 合理性检查

加载器明确强制该组合，技术文档的 30 Hz 节拍一致。

### 依赖影响

所有 tick、速度步长、滤波窗口和停止轮询以 30 Hz 为基准。

### 软件验证结果

静态一致。

### 真机验证状态

CONFIRMED（设计常量，不需真机标定）。

## CAL-003 URDF 资源绑定

### 测量日期

2026-09-10。

### 测量方法

对当前 URDF 文件复算 SHA256，并核对 profile 的相对路径解析层级。

### 原始数据

`sha256sum libs/so_arm_core/so101_new_calib.urdf` = `3a65d2d35e68a8d2f0c2cc176d19b884506543c93ba72980145b80abe276022c`。

### 计算过程

文件字节 SHA256；无需数值拟合。

### 最终值

路径 `../../../libs/so_arm_core/so101_new_calib.urdf`；哈希 `3a65…022c`。

### 单位

路径 / SHA256。

### 写入位置

真机 profile 的 `model.urdf_path`、`model.urdf_sha256`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

从 profile 所在目录解析后指向仓库内现有文件；哈希完全一致。

### 依赖影响

只确认资源身份，不证明实物装配无偏差；实际工具/关节/碰撞参数仍需测量。

### 软件验证结果

PASS（路径与哈希静态检查）。

### 真机验证状态

CONFIRMED（资源绑定）；实物几何为独立待标定项。

## CAL-004 电机校准资源绑定

### 测量日期

2026-09-10。

### 测量方法

对当前只读 `motor_calibration.json` 复算 SHA256。

### 原始数据

文件哈希 `ad315322ad4d19cc322c8f21d86d9667d9df9eca2de325bebbd72fa62deefff5`；权限 `-r--r--r--`。

### 计算过程

文件字节 SHA256。

### 最终值

路径 `motor_calibration.json`；哈希 `ad315…efff5`。

### 单位

路径 / SHA256。

### 写入位置

真机 profile 的 `model.motor_calibration_path`、`model.motor_calibration_sha256`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

profile 哈希与当前文件一致；内容的物理正确性由 CAL-012 单独确认。

### 依赖影响

文件任何变化都会使下游参数哈希及 joints 以后全部阶段证据失效。

### 软件验证结果

PASS（资源完整性）；CAL-012 尚未通过。

### 真机验证状态

CONFIRMED（资源绑定），内容 NEEDS_CONFIRMATION。

## CAL-005 真机串口路径

### 测量日期

2026-09-09（从标定指南附录 A 导入）。

### 测量方法

USB 枚举并通过稳定 by-id 路径定位 CH9102 适配器，随后六台舵机通信。

### 原始数据

`/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14114904-if00` → `ttyACM0`；USB `1a86:55d3`。

### 计算过程

无。

### 最终值

`/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B14114904-if00`。

### 单位

设备路径。

### 写入位置

真机 profile `motor.port`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

by-id 避免 `/dev/ttyACMn` 枚举顺序漂移；已有六 ID 通信记录的文字结论。

### 依赖影响

所有实机采集和运行期串口 I/O。

### 软件验证结果

配置字符串合法；本轮未重新打开设备。

### 真机验证状态

CONFIRMED；原始转储缺口见 CAL-ISSUE-004。

## CAL-006 波特率

### 测量日期

2026-09-09（导入）。

### 测量方法

在 1,000,000 bit/s 对六台逐 ID 通信并读取完整反馈块。

### 原始数据

六台均应答，15 字节反馈块读取成功。

### 计算过程

无。

### 最终值

`1000000 bit/s`。

### 单位

bit/s。

### 写入位置

真机 profile `motor.baudrate`。

### 原代码值

1000000。

### 新代码值

未修改。

### 合理性检查

值在 vendor 波特率表内，且有真机通信结论。

### 依赖影响

影响 I/O 时延以及全部总线测量。

### 软件验证结果

配置合法；本轮未重测总线。

### 真机验证状态

CONFIRMED；原始转储缺口见 CAL-ISSUE-004。

## CAL-007 电机 ID 与轴顺序

### 测量日期

2026-09-09（导入）。

### 测量方法

逐 ID PING，并按公共接口顺序核对五关节加夹爪。

### 原始数据

ID 1~6 全部应答；profile 和官方校准文件均按 `[1,2,3,4,5,6]`。

### 计算过程

无。

### 最终值

`[1,2,3,4,5,6]`。

### 单位

总线 ID。

### 写入位置

真机 profile `motor.ids`；电机校准各条目 `id`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

加载器和 `MotorMapping` 会拒绝两处 ID 不一致。

### 依赖影响

错误映射会使所有位置、电流、速度和阈值张冠李戴。

### 软件验证结果

两份配置静态一致。

### 真机验证状态

CONFIRMED；更换或重新编号电机后立即失效。

## CAL-008 电机型号

### 测量日期

2026-09-09（导入）。

### 测量方法

逐台读取 `Model_Number`。

### 原始数据

六台均为 `777`，vendor 映射为 `sts3215`。

### 计算过程

`MODEL_NUMBER_TABLE["sts3215"] = 777`。

### 最终值

`["sts3215"]×6`。

### 单位

型号标识。

### 写入位置

真机 profile `motor.models`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

六通道一致且 vendor 表具有所需寄存器定义。

### 依赖影响

决定寄存器表、协议、分辨率和编码语义。

### 软件验证结果

静态 vendor 映射一致。

### 真机验证状态

CONFIRMED；原始转储缺口见 CAL-ISSUE-004。

## CAL-009 固件版本

### 测量日期

2026-09-09（导入）。

### 测量方法

逐台读取固件 major/minor 寄存器。

### 原始数据

六台均为 major=3、minor=10。

### 计算过程

格式化为 `3.10`。

### 最终值

`["3.10"]×6`。

### 单位

版本号。

### 写入位置

真机 profile `motor.firmware`。

### 原代码值

当前同最终值；历史仿真值为 2.54。

### 新代码值

本轮未修改。

### 合理性检查

六台一致；旧的电流/速度比例不能随固件字段一起自动沿用。

### 依赖影响

CAL-013、CAL-015、CAL-016 必须按 3.10 重新确认。

### 软件验证结果

profile 已登记 3.10；运行期会逐项核验。

### 真机验证状态

CONFIRMED；原始转储缺口见 CAL-ISSUE-004。

## CAL-010 写应答等级

### 测量日期

2026-09-09（导入）。

### 测量方法

逐台只读 `Response_Status_Level`。

### 原始数据

六台均为 1。

### 计算过程

等级 1 按驱动语义解释为 READ 有应答、WRITE 无设备应答。

### 最终值

`[1]×6`（运行时读回，不存 profile）。

### 单位

协议等级。

### 写入位置

不写配置；初始化时动态读取。

### 原代码值

不适用。

### 新代码值

未修改。

### 合理性检查

与既有静态块读成功以及驱动 level-1 策略一致。

### 依赖影响

写成功只能解释为帧提交串口；CAL-052/CAL-102 必须靠后续反馈和外部观察验证。

### 软件验证结果

Mock 已覆盖 level 0/1/2；本轮未重读真机。

### 真机验证状态

CONFIRMED；原始转储缺口见 CAL-ISSUE-004。

## CAL-011 编码分辨率

### 测量日期

2026-09-10 审计确认（由型号派生）。

### 测量方法

用已确认的 CAL-008 查 vendor `MODEL_RESOLUTION`。

### 原始数据

`MODEL_RESOLUTION["sts3215"] = 4096`。

### 计算过程

编码步长 `360/(4096-1)=0.0879120879°/count`。

### 最终值

`[4096]×6`。

### 单位

count/rev。

### 写入位置

真机 profile `motor.position_resolution`。

### 原代码值

同最终值。

### 新代码值

未修改。

### 合理性检查

大于 1；与型号表一致；单次位置取整误差约 ±0.044°。

### 依赖影响

进入全部 raw↔角度换算和最终行程校验。

### 软件验证结果

PASS（派生与静态表一致）。

### 真机验证状态

CONFIRMED（基于已确认型号和锁定 vendor 表）。

## CAL-014 电流原始零偏（进行中）

### 测量日期

待操作者返回数据。

### 测量方法

保持舵机总线供电、关闭力矩、机械臂无外力且静止，连续只读 30 次六通道 `Present_Current` 原始值；记录实际力矩状态、供电和温度。

### 原始数据

已有历史单次样本 `[0,0,0,0,0,0]`，不足以确定零偏和噪声；新数据待回传。

### 计算过程

每通道以 30 次读数的中位数作为候选 `current_zero_raw`；同时计算 min/max 和 MAD，检查异常编码与噪声。

### 最终值

待定。

### 单位

raw count。

### 写入位置

真机 profile `motor.current_zero_raw`。

### 原代码值

`[0,0,0,0,0,0]`。

### 新代码值

尚未写入。

### 合理性检查

待检查数量、通道顺序、16 位范围、重复离散度以及 CAL-013 符号编码影响。

### 依赖影响

通过后才可标定 CAL-015，并影响 CAL-023、CAL-085、CAL-086。

### 软件验证结果

尚未执行参数写入或回归测试。

### 真机验证状态

CALIBRATING。
