# 自主决策与待审查问题

主 agent 统一维护。每项不确定事项分配稳定编号 `DEC-NNN`；subagent 上交的问题由主 agent 归档于此。
字段：编号与关联 / 问题与证据 / 方案与依据 / 实现与影响 / 验证结果 / 后续审查 / 状态。

## DEC-001 全量回归基线耗时上升（34 用例由构造失败短路变为完整仿真执行）

- 编号与关联：DEC-001；任务 P0-01；P0 文档 §6；dev_logs/upper开发/P0-01_开发日志.md §5；改动 tests/test_grasp_flow.py。
- 问题与证据：基线 34 个 grade TypeError 用例在构造时即失败短路（全套约 151s）；迁移后这些
  用例真正执行完整仿真抓放（虚拟时钟 + 真实 IK），全套约 9.5–10 分钟（341 passed, 571s）。
- 方案与依据：接受耗时上升——这是迁移正确性的直接后果，测试断言未削弱；不为省时而跳过或
  降级抓取仿真用例。
- 实现与影响：无代码差异，仅运行时间；后续各阶段验收命令耗时按 ~10 分钟规划（后台执行）。
- 验证结果：P0-01 与主 agent 独立复跑均为 341 passed / 0 failed。
- 后续审查：若 CI 需要，可审查是否将慢用例标记单独分层；本次不处理。
- 状态：已实现并验证；独立审查结论：待审查。

## DEC-002 run_mock.py 在无原生视觉阶段的装配验证方式（交主 agent 决定，待 P1-05 实施）

- 编号与关联：DEC-002；任务 P1-05/P5-01（预登记）；P5 §2 装配契约；任务.md 视觉边界。
- 问题与证据：P5 §2 规定 run_mock 的 vision"不替换"，使用 P3 原生 source.mode=replay；但本期
  vision.py 为契约桩（P3 实现归队友），原生 replay 无法运行；且"生产入口不得自动回退到替身"。
- 方案与依据（主 agent 预定）：run_mock.py 装配保持 P5 §2 的顺序与契约、vision 配置路径照常
  预检；pytest E2E（tests/test_integration_mock.py）在测试进程内用 tests/support_upper.py 的
  FakeVision 替换 vision 模块引用后复用 run_mock 的装配函数与生产 run_session——不复制业务循环、
  不改 vision 桩语义（直接跑 run_mock 主函数在视觉桩上按契约明确失败）。
- 实现与影响：main.py 与 scripts/run_mock.py 的装配顺序按 P5 §2 契约；pytest E2E（P5）注入 FakeVision 复用生产 run_session 与装配函数，不复制业务循环；直接跑 `python scripts/run_mock.py` 在 vision.configure 处抛 VisionHardError → terminate("VISION_INIT_FAILED") exit 1，不静默回退。
- 验证结果：2026-09-23 P1-05 落实——tests/test_main_flow.py 27 passed；run_mock 冒烟实测 VISION_INIT_FAILED 退出路径（logs/mock/run-*.jsonl startup 哈希正常）；全量 702 passed。
- 后续审查：审查装配复用度与"生产不自动回退替身"是否守住；P3 交付后切换原生 replay 的改动点。
- 状态：待实现（预登记决策）。

## DEC-003 preflight 的 secrets 定位与异常分层（P1-01 已实现并验证）

- 编号与关联：DEC-003；任务 P1-01；P1 §3.1/§3.2 第 7 条；dev_logs/upper开发/P1-01_开发日志.md §7（DEC-P1-01-A/B）。
- 问题与证据：P1 §3.2 第 7 条要求生产预检查两个 API key 非空，但 §3.1 冻结的
  `preflight(cfg, params, registry)` 签名不含 secrets 参数；规格同时把 secrets 位置固定为
  `configs/secrets.local.json`（`__file__` 定位）。
- 方案与依据：实现按签名不变，preflight 内部读模块级 `SECRETS_LOCAL_PATH`（测试可
  monkeypatch）；失败抛 `PreflightError(AppConfigError)` 携带稳定 `check` 标识。备选"加第 4
  参"违反冻结签名，否决。
- 实现与影响：configs/app_config.py（`AppConfigError/PreflightError/preflight/_check_secrets`）；
  P1-05 main 可单 catch AppConfigError 并映射 terminate。
- 验证结果：tests/test_app_config.py 128 用例通过；全量回归 469 passed 独立复跑一致。
- 后续审查：审查"preflight 读文件副作用"是否可接受、check 标识与 terminate 状态映射的完整性。
- 状态：已实现并验证；独立审查结论：待审查。

## DEC-004 配置层工程约定汇总（P1-01 已实现并验证）

- 编号与关联：DEC-004；任务 P1-01；P1-01 开发日志 §7 DEC-P1-01-C/D/E/F/G/H。
- 问题与证据与方案：① registry 用 `Mapping[str, type]` 鸭子类型检查（BasePlan 属 P1-04，
  不反向 import）；② 路径 `.resolve()` 绝对化、加载器不查存在性（存在性属预检）；③ 预检
  收窄形态：vision JSON 顶层须为对象、prompt strip 非空、mock 脚本=文件/帧目录=目录、
  log_dir 只 `os.access` 不写探针；④ 负向断言比对结构化 `field_path/message`（防 tmp_path
  目录名意外满足关键词断言）；⑤ load_secrets 拒绝未知键、允许空串（非空属生产附加）；
  ⑥ 导出 `PRODUCTION_APP_PATH/MOCK_APP_PATH` 常量钉住 D15 固定入口。
- 实现与影响：configs/app_config.py 与 tests/test_app_config.py；P1-05 复用路径常量。
- 验证结果：同 DEC-003（13 处变异测试逐条确认断言咬合力，见日志 §3）。
- 后续审查：mock 入口是否需要读取 secrets 的边界（当前跳过）；P2 若扩展密钥字段须同步
  SECRET_KEYS 与模板。
- 状态：已实现并验证；独立审查结论：待审查。

## DEC-005 开发机安装 httpx（P2 适配层测试依赖），其余 P2/P3 重依赖不装

- 编号与关联：DEC-005；任务 P2-02/P2-04；P2 §2/§3.2；任务.md §视觉分工（不为离线验收安装相机/YOLO 专用依赖）。
- 问题与证据：P2 明确"两家均用显式 REST 适配（httpx）"且 T17 要求"127.0.0.1 本地服务子进程做真实墙钟测试"，开发机 conda env Qingyun 无 httpx；sounddevice/onnxruntime/silero/torch 亦无（P2-01 已用注入替身离线验证，无需安装）。
- 方案与依据：`pip install httpx`（0.28.1，纯 Python 轻量，属本任务云适配必需测试依赖，非相机/YOLO 专用依赖）；音频/VAD 重依赖与视觉依赖一律不装，保持注入替身验证路线；requirements.txt 的最终增补与版本锁定归 P2-04（目标机验证状态如实标注）。
- 实现与影响：环境变化仅开发机 conda Qingyun；requirements.txt 由 P2-04 按规格增补。
- 验证结果：httpx import 正常（0.28.1）。
- 后续审查：目标机/香橙派上 httpx 版本锁定与 signal 可中断性实测（P2 §3.2 要求）。
- 状态：已实现并验证；独立审查结论：待审查。
