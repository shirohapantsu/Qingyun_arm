# P0-02 开发日志：P0 历史文档同步清单

## 1. 任务与依据

- 任务 ID：P0-02。目标：完成 P0 遗留的"历史文档同步清单"，使历史文档与已冻结的
  现行规格（P0–P5 实施文档 + README 决策 D01–D20）一致。**纯文档修订，不改任何 .py
  代码、prompt.md 或测试**。
- 规格依据：`docs/实施文档/P0_公共契约与迁移技术文档.md` §4 表格第 5 行"文档同步清
  单"（初审 D 类 / 复审 R01，标注"全部在本阶段完成"）；决策依据 `docs/实施文档/README.md`
  D01/D05/D06/D07/D08/D10/D13/D14/D15；现行接口以 `configs/common_interface.py`（六字段
  `VisionInterface` + 新 `VisionThresholds`）与 `qingyun/grabbing/arm_control.py::_validate_target`
  实际代码为准。
- 前置：P0-01 已验收——`arm_control._validate_target` 删除 grade、接口层 grade 引用清零、
  `VisionThresholds` 落地、`prompt.md` 收窄为 strawberry/invalid。本任务在其之上同步文档。
- 视觉边界：不实现任何视觉内容；doc② 中只同步文字语义。

## 2. 实现说明（文件 + 关键改动 + 决策依据）

改动仅限 5 份历史文档 + 运动文档（见 DEC-P0-02-A）。每处修订标注决策编号与日期 2026-09-23。

### ① `docs/上层任务编排模块技术文档.md`（5 处）
- §2 vision 签名块：在 `class VisionHardError` 与 `def init()` 之间补
  `def configure(thresholds: VisionThresholds, config_path: Path) -> None` 一行 + 注释
  （D13；精确签名对齐 `docs/实施文档/P3_视觉检测与目标选择技术文档.md` §3 第 35 行）。
- §2 cloud_model 签名：`返回 "strawberry"|"blueberry"|"invalid"` → `本期合法返回
  "strawberry"|"invalid"（D07：本期仅草莓，蓝莓口令归 invalid）`。§1 第 20 行已是
  `{"task": "strawberry"|"invalid"}`，与 §2 一致（D01/D07）。VisionInterface 字段描述
  （§2 get_target 注释携带 valid_count）与六字段契约一致，未改。
- §6 恢复流程 / 失败分发：**核对完整性，未改**。§6 第 111→118-119、130 行已带
  D08/D14 修订（运动后 IK_FAILED/PLAN_INVALID → terminate、封顶 → terminate、
  NoTarget(skip_exhausted) 不属封顶），header 第 9 行标注"§1/§2/§5/§6 已同步"。
- §5 / §9 内旧"恢复后继续"文字：grep 核实 §5（第 105 行已 D14 废止划线）、§9（环境
  假设，无封顶/恢复文字）均无残留。
- §10 已知风险 #7（过小目标）："…→ ignore 封顶穷尽（§6）→ **任务完成**" 与 §11
  评审结论 #7"重试至 ignore ≥ valid_count → **任务完成（穷尽式正常出口）**"：均为
  D14 之前的旧封顶出口表述，与本文 §6 现规范（terminate）矛盾。按 D14 同步：§10 改
  `→ terminate("NO_GRASPABLE_TARGET")`；§11 对旧句加 ~~划线~~ + "**D14 已废止（2026-09-23）**：
  改判无可抓取目标 → terminate，不再是正常出口（见 §6）"。
- §11 待办清单第 193 行：`运动层 grade 校验移除与 tests 构造点修订随 P0 收尾` →
  `…已在 P0-01 完成（2026-09-23 P0 实施：arm_control._validate_target 只校验
  position/yaw，接口层 grade 引用清零）`，保留 `[x]` 勾选。

### ② `docs/视觉-plan-运动方案设想.md`（2 处 + 1 核实）
- §一 第 21 行：`目标为运动侧运行代码零引用（当前 arm_control._validate_target 仍引用
  grade，P0 移除后成立）` → 改写为当前事实 `运动侧运行代码已零引用 grade
  （arm_control._validate_target 现只校验 position/yaw；2026-09-23 P0 完成）`。
- §三 第 75 行：`过小目标逐个 MISS → 封顶 → 任务完成` 属 D14 之前的旧封顶出口，与本文
  §五（第 88 行 D14 修订）/§八（第 130 行 terminate）冲突。按 D14 同步为
  `→ terminate("NO_GRASPABLE_TARGET")`，并注明"原'封顶→任务完成'已废止，见 §五/§八"。
- §五 封顶出口语义：**核实已同步，未改**。第 6 行 header 有 D14 注记，第 88 行正文已是
  `封顶（D14 修订）… terminate("NO_GRASPABLE_TARGET")（原"解锁复位后正常返回"已废止）`，
  §五正文（85–91 行）无残留"封顶→正常完成"。

### ③ `docs/上层视觉运动完整功能落地方案.md`（3 处 + 完整性核对）
- §6.3 尺寸行（第 244 行）：核实已带 D10 逐轴比较标注（`length_m ≤ envelope[0] 且
  width_m ≤ envelope[1]，本行原"投影尺寸"表述作废；不新增尺寸下限`），未改。
- §6.3"其余尺寸"行（第 245 行）：原"建议补充：内部点云同时验证长、宽、高不超完整运动
  包络"已被 **D10 否决**（视觉仅逐轴闭合两维上限、不设下限、不查高度维）；改注为
  D10/D18 事实语义——实物长宽高由 **D18** 演示样本前提保证（不逐个量具登记），运动不为
  超包络兜底（R06），超高目标引发 COLLISION 按直死 terminate。
- §7.4 恢复矩阵：**核对完整性**。表格上方 blockquote（第 345 行，2026-09-22）已对
  "两行 IK_FAILED/PLAN_INVALID recovery_required=True"加 D08 废止、对"严格人工确认
  协议"加 D13 否决、对 ignore 封顶加 D14 terminate。表格下方两段（原为旧表述）随文加
  2026-09-23 修订：人工确认段改为"维持回车即确认、EOF/Ctrl-C 照常 terminate"（D13）；
  封顶恢复段"必须恢复成功才打印完成"加 ~~划线~~ + D14（封顶不再恢复、不打印完成、
  直接 terminate、臂冻结；本表恢复只适用 GRASP_MISS/UNCERTAIN/SLIP，IK_FAILED/PLAN_INVALID
  运动后已 D08 terminate）。
- §1.1"已核对的仓库事实"表（第 31 行下）：该表为 2026-09-22 初核快照，其中"`_validate_target`
  仍要求 grade""tests 仍有 grade 构造"两项现由 P0-01 完成。保留原历史表格（不推翻），
  在表下说明句追加 2026-09-23 更新注记，指向现行运动文档 §3.2 / 公共接口为准。

### ④ `readme.md`（三处）
- §3 接口表 + 正文：接口表第三列由 `目标品级 / 记录用元数据 / A+、A、B、C / 字符串 /
  "A+"` 改为 `视觉附加元数据`（length/width/ripe/valid_count，供 plans 分级与 ignore
  封顶使用、运动不读取）；`VisionInterface(position, yaw_deg, grade)` → 六字段现行契约
  `VisionInterface(position, yaw_deg, length_m, width_m, ripe, valid_count)`；正文
  "不根据 grade 选择放置位置" → "不因视觉元数据（length/width/ripe/valid_count）改变
  放置选择"（与运动文档 §8 一致）。
- 工程结构：`qingyun/watchdog.py` → `qingyun/shutdown.py`（状态表第 26 行 + 文件树第
  53 行），注明"P1 删除原 watchdog 新增 shutdown"（对齐上层文档 §2 shutdown.py 命名）。
- 行数统计：按 `wc -l`（2026-09-23）更新明显过时项——`motor_control.py 1966→2104`、
  `arm_control.py 949→1015`（P0 迁移及后续改动）、`safety.py 711→718`；`kinematics_ext
  580 / executor 379 / trajectory 314` 与实际一致未改；`common_interface / motion_params`
  由"—"补为"159 / 1441"。两处表头日期 2026-09-09 → 2026-09-23；0 字节占位/待实现状态
  按实况（main/vision/asr/cloud_model 仍 0 字节，shutdown 待新增）保留。

### ⑤ `docs/真机参数测量与标定指南.md` 附录 A.2 第 1 条
- **核实已同步，未改**。该条所指 profile 为 `calibration/REAL_ARM/PC/profile.json`
  （`robot_id=REAL_ARM_PC`）。现文已表述为事实状态："`status` 与 `verification` 矛盾
  已解除：配置已退回 `draft`（`verification: null` 与 draft 一致；2026-09-22 复核与实际
  文件一致，A.4 第 1 条为最新状态）…退回 draft 不影响采集链"。与实际文件核对：
  `profile.json` 第 5 行 `"status": "draft"`、第 1031 行 `"verification": null`，A.4 第 1 条
  第 697 行"`status` 已为 `draft`"，三者一致。符合"已在早前轮次同步完成，记录证据保持
  不动"。

### ⑥ `docs/机械臂运动控制模块技术文档.md`（2 处，见 DEC-P0-02-A）
- §3.2"视觉数据"第 80 行：`grade 是记录用元数据，不参与本函数的抓取目标或放置位姿
  选择。入口检查数组 shape (3,)、float64、有限值、角度范围和字段类型…` 属"描述目标接口
  字段/运动读取"的 grade 残留，且 `_validate_target` docstring 明确引用"文档 3.2"。同步
  为六字段事实：`length_m/width_m/ripe/valid_count 是视觉附加元数据…运动入口不读取…入口
  只检查 position 的 shape (3,)、float64、有限值，以及 yaw_deg 的数值类型与角度范围`，与
  代码 `arm_control.py:414-440` 逐字一致。
- §8 必测项第 270 行：`place_id 只读取配置，不根据 grade 选择` → `不因视觉元数据
  （length/width/ripe/valid_count）改变放置选择`（与 readme §3 一致）。

## 3. 验证记录

- 工作目录：`/home/shiroha_pantsu/labroratry/Qingyun_arm`
- 解释器：`/home/shiroha_pantsu/miniconda3/envs/Qingyun/bin/python`（conda env Qingyun，Python 3.10）

| # | 命令 | 结果 | 退出码 |
|---|---|---|---|
| V1 | `python -m pytest tests/test_motion_params.py tests/test_safety_geometry.py -q` | **85 passed, 1 warning in 22.39s**（唯一 warning 为第三方 hppfcl→coal 弃用，与本任务无关） | 0 |
| V2 | `grep -n "grade" docs/上层任务编排模块技术文档.md docs/机械臂运动控制模块技术文档.md readme.md docs/视觉-plan-运动方案设想.md` | 见下表逐条分类：全部为 plans 内部分级概念或"grade 已删除/迁移完成"事实句；**机械臂运动文档 0 处、readme 0 处** | 0（有命中，命中均合规） |
| V3 | `grep -rn "\.grade\|grade=\|grade:" configs qingyun` | **无匹配**（确认 P0-02 未触碰生产代码，P0-01 的接口层 grade 清零状态保持） | 1（grep 无命中） |
| V4 | `git diff --stat` | 本次仅 5 份 .md（上层任务编排/落地方案/运动/视觉-plan/readme）改动；`configs/common_interface.py`、`prompt.md`、`arm_control.py`、`tests/*.py` 的 diff 为 P0-01 既有未提交工作，本任务未编辑 | 0 |
| V5 | `ls --time-style`（mtime 对比） | P0-01 的 .py：common_interface 01:18 / arm_control 01:19；P0-02 文档：视觉-plan 02:11 / readme 02:14 / 运动文档 02:15——.py 时间戳早于本次，未被改动 | 0 |
| V6 | `wc -l`（行数核对） | motor_control 2104 / arm_control 1015 / safety 718 / kinematics_ext 580 / executor 379 / trajectory 314 / common_interface 159 / motion_params 1441；main/vision/asr/cloud_model/watchdog 均 0 字节，shutdown.py 尚不存在（P1 新增） | 0 |
| V7 | `grep "status"/"verification" calibration/REAL_ARM/PC/profile.json` | 第 5 行 `"status": "draft"`、第 1031 行 `"verification": null`——支撑 ⑤ 保持不动的证据 | 0 |
| V8 | `grep -n "^def configure" docs/实施文档/P3_…技术文档.md` | 第 35 行 `def configure(thresholds: VisionThresholds, config_path: Path) -> None`——① 补入签名与之逐字一致 | 0 |
| V9 | 每份文档 `git diff <文件>` 自查 | 5 份文档 + 运动文档逐处上下文通顺（见 §2） | 0 |

- 说明：未运行 `test_grasp_flow.py` 全量（单文件约 8 分钟），按任务许可改用
  V1 快检组合证明代码未被误动，并以 V3/V4/V5 三重证据说明 .py 零改动。

### V2 grade 残留逐条分类（合规判定）
- 上层文档 8 处：`grade 判定（plans 内部）`（66/101/102）、`next_place_id(grade)`（66/70）、
  `grade→place_id 日志（grade 不出 plans）`（69）、`grade 同样不出 plans`（100）——均为
  plans 内部分级概念，允许保留；`运动层 grade 校验移除…已完成`（193）、`VisionInterface
  删 grade`（203）——均为"grade 删除/迁移完成"事实句，允许保留。
- 视觉-plan 8 处：§六 plans 的 `grade 判定 / grade=… / next_place_id(grade) / grade==A/B/C
  / grade→place_id 日志（grade 不出 plans）`（95/98/102/106/107/111/113）——plans 内部，允许；
  第 21 行"grade 字段删除…运动侧运行代码已零引用 grade"——迁移完成事实句，允许。
- 机械臂运动文档 0 处（80/270 已同步）；readme 0 处（122/124 + 接口表已同步）。
- 结论：无任何 grade 残留把 grade 描述为"现行目标接口字段"或"运动读取字段"。

## 4. 覆盖关系（P0 §4 清单 ①–⑤ → 证据）

| 清单项 | 处理 | 证据 |
|---|---|---|
| ①上层文档 §6 D08 废止标注 + §11 勾销 + §2 configure 行 + §1/§2 六字段/strawberry-invalid | 已完成 / 核对完整 | §2 补 configure（V8）、§2 task 收窄、§11 第193行勾销、§6 header 118-119 D08 完整；§10/§11 残留封顶出口随文 D14 修订 |
| ②视觉契约 §一删除错误"仍引用 grade"表述 + §五 D14 改写 | 已完成 + 核实 | §一第21行改为当前事实；§五正文核实无残留（第88行已 D14）；§三第75行同类残留一并 D14 同步 |
| ③落地方案 §7.4 D08/D13/D14 标注 + §6.3 尺寸逐轴 | 已完成 + 核对完整 | §6.3 第244行核实已 D10 逐轴、第245行 D10/D18 改注；§7.4 blockquote 已 D08/D13/D14，随文两段补 D13/D14（2026-09-23） |
| ④readme §3 接口表 grade / 工程结构 watchdog→shutdown / 过时行数 | 已完成 | §3 六字段+元数据 phrasing；watchdog→shutdown（26/53）；行数 2104/1015/718 更新（V6） |
| ⑤标定指南 A.2 第1条"已退回 draft"事实状态 | 核实已同步，保持不动 | 现文已述"配置已退回 draft"；与 profile.json status=draft/verification=null（V7）、A.4 第697行一致 |
| 附加：运动文档"目标接口字段/运动读取"grade 残留（验证 grep 覆盖） | 已完成 | §3.2 第80行、§8 第270行同步；V2 显示运动文档 0 处；代码零改动（V3/V4/V5） |

未验证项：无（本任务为文档同步，无新增必测代码；V1 快检 + V2/V3/V7 grep 为完成判据）。

## 5. 问题与限制

- 无失败、无缺依赖、无被跳过的必需检查（V1 全通过，V2/V3/V7 grep 结论明确）。
- 视觉边界：仅改 doc② 文字语义，未实现/改动任何视觉逻辑或 `vision.py`（仍 0 字节占位）。
- 未做 git commit / push（遵守禁止）。P0-01 的 .py/configs/prompt 未提交改动保持原样，
  本任务只叠加 5 份文档改动。
- 保留的分级/grade 相关文字（plans 内部 grade、§2 grade 判定）是规格明确允许的概念，非缺陷。

## 6. 交接事项（供主 agent 重点审阅）

- 主 agent 应审阅 DEC-P0-02-A（运动文档纳入范围）与 DEC-P0-02-B（readme/上层 §4 的
  blueberry 历史句处理），确认解读与验收口径一致。
- P1/P3 消费者文档已一致：`vision.configure(thresholds, config_path)`（P3 §3 / 上层 §2）、
  六字段 `VisionInterface`（readme §3 / 运动文档 §3.2 / 视觉契约 §一）、D08/D13/D14 恢复
  与封顶语义（上层 §6 / 落地方案 §7.3–7.4 / 视觉契约 §五）。
- 台账与自主决策记录文件（`dev_logs/upper开发/任务台账.md`、
  `…/自主决策与待审查问题.md`）由主 agent 维护，本 subagent 未改动，仅在此登记 DEC 供归档。
- `scripts/calibrate.py` 中模拟审计记录字段 `"grade"`（P0-01 遗留、允许保留）不在本任务
  grep 目标文档内，未触碰。

## 7. 自主决策

- **DEC-P0-02-A（运动文档纳入 P0-02 范围）**：任务"修改范围"编号 ①–⑤ 列 5 份文档，未列
  `docs/机械臂运动控制模块技术文档.md`；但 P0 §4 清单第 5 行"文件"列明确写"原两份契约、
  **运动文档**、README"，且本任务"验证要求"的 grade grep 逐条覆盖该文档并规定"凡描述
  '目标接口字段/运动读取'的 grade 残留须已同步"。§3.2 第 80 行与 §8 第 270 行正是描述
  grade 作为目标接口字段/运动读取的残留（`grade 是记录用元数据`、`不根据 grade 选择`），
  且 `_validate_target` docstring 直接引用"文档 3.2"。若不同步则验证判据不满足。故据
  验证判据 + P0 §4"运动文档"行，将这两行同步为六字段事实（与代码逐字一致），改动最小、
  不触碰其余内容。关联 §2⑥、V2。
- **DEC-P0-02-B（历史 blueberry/数字 task 句保留）**：readme §3 表头/正文按 ④ 明确指示已
  改六字段与 strawberry/invalid；readme §2 分工"输出…目标品级"为 ASCII-grep 不命中的
  早期角色描述、且不在 ④ 三处清单内，保持不动。上层文档 header 第 6 行 D01"strawberry/
  blueberry/invalid"与 §4 第 90 行"strawberry/blueberry"是带日期的决策/背景记录（第 8 行
  D07 已注明本期收窄），非现行合法值声明；本任务对 task 合法值的同步按 ① 明确指示限定在
  §1/§2，未回改这些历史/背景句，以免"推翻文档其余内容"。
- **DEC-P0-02-C（D08 §6 标注完整性判定为已达标）**：§6 表格行"IK_FAILED/PLAN_INVALID… 重试类，
  见下"经"见下"第 118-119 行的 recovery_required 分流（True→terminate，False→跳选）已完整
  表达 D08 废止，无需再改表格行本身（"若已有标注则核对完整性"→判定完整）。
- **DEC-P0-02-D（落地方案 §7.4 无 main-CLI 建议项）**：任务括注"D13：main CLI 项已被 D15
  作废"是提示判断 §7.4 中哪些建议项被后续决定覆盖；核实 §7.4（恢复矩阵）不含 main CLI
  建议项（该项在 §11 入口参数，已按 D15 用 `scripts/run_mock.py` 独立入口呈现，不在 ③ 的
  §7.4/§6.3 范围）。§7.4 实际被覆盖的 D13 建议项是"严格人工确认协议"（已加 D13 修订）。
- **DEC-P0-02-E（封顶残留跨节一并同步）**：doc① §10/§11、doc② §三 各有"封顶→任务完成"
  旧句，与本文档 §6/§五/§八 现规范（terminate）自相矛盾。虽 ① 指示聚焦 §6、② 聚焦 §一/§五，
  但任务总目标为"与已冻结现行规格一致"且验证要求 D14 封顶语义，故按 D14 一并随文标注同步
  （§11 用划线保留历史记录，§10/§三 直接改出口），未改写任何验收规格迁就实现。
- **DEC-P0-02-F（落地方案 §1.1 初核快照处理）**：§1.1"已核对的仓库事实"表第 23/24 行仍写
  "`_validate_target` 仍要求 grade""tests 仍有 grade 构造"——是对"运动读取"的 grade 描述，
  现与 P0-01 事实不符。该文档不在任务 grade 验证 grep 的四文件清单内、③ 的逐条指示也仅列
  §7.4/§6.3，且表格自我标注为"静态核对结果"（2026-09-22 初核快照）。处理方式：不删改历史
  快照单元格（遵守"不推翻文档其余内容"），仅在表下说明句加 2026-09-23 更新注记，声明两项
  已由 P0-01 完成、现行以运动文档 §3.2/公共接口为准。若主 agent 认为应直接改写表格单元格，
  可在此 DEC 基础上返工。
