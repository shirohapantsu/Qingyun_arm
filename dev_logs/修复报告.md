# SO-ARM101 抓取—放置模块 缺陷修复报告

> 修复日期：2026-09-08
> 依据：`docs/` 三份技术文档 + `代码审查报告.md`
> 信息优先级按 prompt.md 第八节执行；Reviewer 结论逐条先确认真实性再修。

## 一、逐项处理结果

### P1-1 桌面/障碍判定缺少 clearance —— 已修复

- **根因**：`_table_inflation`、`_obstacle_margin` 在构造器里算好后从未接线到检查；报错文案与实际判定不一致。clearance 本应从 `ErrorEnvelope.total_link_margin_m` 进检查，但该属性也是死代码。
- **修改文件**：`qingyun/grabbing/safety.py`
- **修改方式**：
  - 桌面判定改为 `d - r - (table_flatness_m + clearance_m) < 0` 才罚，消息改为陈述实际预算（`(r + flatness + clearance)*1000` mm）；
  - 障碍判定改为 `d - r - clearance_m < 0`，消息同样标注预算；
  - 非接触阶段"连杆×目标包络"同步要求 clearance 净距（指南 2.2：clearance 是"误差膨胀以外"的通用几何净距，不只障碍/桌面）；
  - 删除从未被消费的 `total_link_margin_m` / `total_object_margin_m` 死属性。
- **测试**：重写 `test_净距要求参与判定而不只是相交`（薄墙 `half < gap < clearance`，先断言几何确不相交再断言判罚；附"墙推到 clearance 外必须放行"的对照组）；新增 `test_桌面判定扣除平度与净距`。修复前两个新判据均不成立（旧实现阈值实测 0mm），修复后通过。

### P1-2 `_sweep_inflation` 混用坐标系 —— 已修复

- **根因**：`_axes` 把 link 系局部端点 `cap.p0_m/p1_m` 传给用基座系关节轴算垂距的函数。
- **修改文件**：`qingyun/grabbing/safety.py`
- **修改方式**：`_axes` 改传已换算的基座系端点 `(a, b)`（`_axes` 内本来就为胶囊体算了这两个值）。
- **测试**：新增 `test_扫掠余量用基座系端点计算`——对 home 姿态逐胶囊体用基座系端点解析重算扫掠膨胀并与 `_axes` 输出半径对比（abs=1e-9）。旧实现下 link 系与基座系数值不同源，该断言失败。

### P1-3 接触豁免过宽 —— 已修复（实现方式与 Reviewer 建议略有不同，见说明）

- **根因**：`object_contact_allowed=True` 时 4.4 节整段跳过，任何连杆压到目标都不报。
- **修改文件**：`qingyun/grabbing/safety.py`
- **修改方式**：新增模块常量 `GRIPPER_CONTACT_LINKS`（`gripper_link` / `gripper_frame_link` / `moving_jaw_so101_v1_link`，即 wrist_roll 之后的夹爪子树）；接触阶段仅豁免挂在这些 link 上的胶囊与目标的检查，腕及以上连杆在任何阶段都必须与目标保持膨胀+净距。文档字符串同步更正。
- **为什么不采用"只豁免 id 含 pad 的胶囊"**：Reviewer 给了两个候选（配置加 `is_pad` 字段 / checker 内按 id 约定）。加字段会改变指南 2.2 定义的 capsule schema（`{id, link, p0_m, p1_m, radius_m}`，且加载器拒绝未知键），按信息优先级弃用。而实测表明：抓取位姿下 `tool_head`（固定指座胶囊）距目标膨胀盒 -20mm——指座/活动指与指垫是同一刚体组件，抓取时必然进入目标的误差膨胀包络；只豁免"指垫"会让文档规定的正常抓取永远报 COLLISION。故把豁免组件定为"夹爪子树全部胶囊"，腕及以上仍罚。Reviewer 的复现场景（目标盒压 `lower_arm` 轴，contact=True）修复后被捕获（新测试第一条断言）。
- **测试**：`test_持物包络挡住去路时被捕获` 更新为新语义（大盒压在臂体上，接触阶段同样罚）；新增 `test_接触阶段只豁免抓取组件不豁免臂连杆`（复现 + 正/反向用例：贴指垫的小盒接触阶段放行、非接触阶段判罚）。

### P1-4 CHECK 未预规划整条名义路线 —— 已修复

- **根因**：`_preplan` 只构造 APPROACH+DESCEND；抓取后的 5 段与放置位姿可达性在闭爪后才首次规划。
- **修改文件**：`qingyun/grabbing/arm_control.py`
- **修改方式**：
  - `_replan_after_close` 的路径构造+校验拆为 `_build_post_grasp_steps(position, yaw_deg, place, q_ref, g_close)`；
  - `_preplan(position, yaw_deg, place)` 在现有两段校验后，按 `contact_gap_range_m` 区间**两端对应开度**各做一次 IK 并把 lift/transfer/lower/retreat/return 全部构造+校验（含 `_check_grasp_pose(T_place)`），即文档 5.3"包括已标定接触开度区间内的 TCP/持物变化包络"；
  - 闭爪后 `_replan_after_close` 按实测 g 重建再校验（同一构造函数，两步都做，与文档原文一致）；
  - `_preplan` 调用处把 `CollisionViolation` 也纳入 `_early_fail`：预规划失败且臂未动 → `recovery_required=False`、不锁存（文档 4.3）。
- **测试**：新增 `test_放置位姿不可达时在CHECK就被拒绝`——把 `places.default` 改到绝对放不到的位置后调用：断言 `stage=CHECK`、`recovery_required is False`、`motor.command_count == 0`。修复前该场景要到闭爪后才失败（command_count>0 且 recovery=True），复现成立。

### P1-5 实机标定链路三处缺陷 —— 已修复

- **缺陷 1（status 死锁）根因**：`_open_motor_controller` 走 `load_motion_params(mode="real")`，而采集发生在 draft 期且大量运行期字段为 null——real 链路在结构上不可能满足。
  - **修改**：`configs/motion_params.py` 新增 `CalibrationProfile` + `load_calibration_profile()`：只解析采集必需的最小字段集（motor 组身份/分辨率/串口/官方校准文件；joints 未拟合时返回 None；电流与速度比例缺测时以 NaN 登记，用到即报错，不猜值）；接受 `status ∈ {draft, verified}`，明确拒绝 simulation；不核对哈希（标定包里这两份文件本来就在更新）。
- **缺陷 2（`read_raw_snapshot` 虚构）**：
  - **修改**：`qingyun/grabbing/motor_control.py` 新增 `CalibrationReader`：只读总线（无 `send_action`/力矩/保持等任何提交路径），一次绝对 deadline 覆盖六台全部事务（与运行期同一原则，预算独立为采集级 `_CALIB_READ_TIMEOUT_S`），`read_snapshot()` 返回 raw_position/velocity/current（寄存器原始值）+ 起止 ns 时间戳。
- **缺陷 3（`fb.bus_degrees` 虚构）**：
  - **修改**：`read_snapshot()` 同时给出 `q_bus_deg`（4.2 第一行公式，经 `MotorMapping.raw_to_bus_deg`，只依赖官方校准的行程中点，不需要 joints 已拟合）与 `q_urdf_deg`（仅在 joints 组已拟合时用共享函数 `bus_deg_to_urdf_deg` 换算，否则 None——绝不用占位 sign/offset 输出假角度）；`gripper_pct` 在端点未标定时返回 None。
  - `scripts/calibrate.py`：`_capture_hardware` 全面改用 reader 并 `finally: reader.close()`；joints 采集用 `snap["q_bus_deg"]`；tool/workcell 用 `q_urdf_deg`（未拟合时给出指向 joints 阶段前置的明确错误）。
- **附带修复（同一条链上的第二处死锁，Reviewer 未发现）**：`_check_hardware_prerequisites` 原先要求非 motor 阶段的 `timing.read/write_timeout_s` 非 null——但 timing 组要到第 5 阶段才拟合，tool/workcell 采集必然再次死锁。改为：所有阶段（含 motor）要求 `motor.port`/`motor.baudrate`，不再要求 timing 字段（只读采集用 `CalibrationReader` 自己的预算）。`load_calibration_profile` 里官方校准骨架（range 为 null）会被明确拒绝，指引操作者先放入官方文件。
- **测试**（`tests/test_calibration.py` 第 6 节，假总线，无需硬件）：`test_draft配置可被采集入口加载`、`test_采集入口拒绝模拟包与缺失串口`、`test_capture_hardware_motor_stage`（含"全程无写帧"断言）、`test_capture_hardware_joints_stage给出q_bus`（与生产换算层逐值一致）、`test_capture_hardware_tool阶段在joints未拟合时明确拒绝`。

### P2-1 坏测试与实现共享盲区 —— 已修复

见 P1-1。薄墙构造 + 几何不相交前置断言 + 对照放行用例 + 桌面净距专项。

### P2-2 `object_shift_bound_m` 单位混淆 —— 已修复

- **根因**：`slip_drift_pct`（开度百分比）×0.001 冒充米。
- **修改文件**：`scripts/calibrate.py`（`_fit_grasp`）、`docs/参数标定与测量手册.md`（附录 C 状态更新）
- **修改方式**：优先采用试验汇总里直接测得的 `center_shift_m`（"最大值 + 1mm 测量余量"）；只有开度漂移记录时按 `gap_table` 斜率把漂移换算为内间距变化量的保守上界再加余量；两者都缺时保持未完成状态不写值。slip 阈值（开度百分比）逻辑独立保留。
- **测试**：`test_object_shift_bound必须是米量级的实测位移`——两条路径用精确期望钉住（实测 4mm→0.005；漂移 3%×斜率→0.0025；旧实现给 0.003 且含义错误）。

### P2-3 fit 写猜测值 —— 未披露的一处已修复；12 处自认项维持原状

- `motor.port` 为 null 时 fit 不再写 `/dev/ttyUSB0`（`_fit_motor`），保持 null；hardware 采集 metadata 现在记录真实端口，fit 从 metadata 转写。指南 3.3 前置检查也要求操作者先填端口。
- 手册附录 A 已披露的 12 处常数/占位（`current_zero_raw=[0]*6`、超时常数等）维持——文档已声明其实机必须人工覆盖，不属于"未披露冒充标定结果"。
- `tests/test_calibration.py` 的 pipeline fixture 相应改为"操作者在 init 后填入端口"（模拟真实流程），并新增断言链保证 promote 产物仍可被 real 加载。

### P2-4 原语在"臂未动"的失败上锁存 —— 已修复

- **修改文件**：`qingyun/grabbing/arm_control.py`
- **修改方式**：`_enter` 每次调用复位 `_motion_started`；`move_joints` / `move_cartesian_top_down` / `close_gripper` 的 except 分支改为 `if self._motion_started: self._latch()`（`close_gripper` 补设 `_motion_started = True`，它进入即在提交）。与 `grasp_and_place._early_fail` 语义对齐（文档 4.3）。
- **测试**：`test_入参校验失败不锁存公开原语`（shape 错 / 越限 / 非竖直三种失败后 `_fault_latched is False`、`command_count == 0`、随后正常运动可用）。

### P2-5 标定工具重复实现且丢"禁止外插" —— 已修复

- **修改文件**：`scripts/calibrate.py`
- **修改方式**：删除本地 `np.interp` 副本，`_pct_for_gap` / `gripper_pct_to_gap_simple` 改为经行包装（`_gap_table_params`）转调生产实现 `gap_to_gripper_pct` / `gripper_pct_to_gap`（越界报错，CLI 层转为可读 `CalibError`）；删除 7 个死导入（`R_TOP_DOWN`、`gripper_pct_to_angle_deg`、`inverse_transform`、`rz_deg`、`tool_transform`、`top_down_pose`、以及失去调用方的顶层 `load_motion_params`）。
- validate 的 grasp 阶段先算两条显式覆盖检查（"接触区间被 gap_table 定义域覆盖"、"预张开度被 gap_table 定义域覆盖"），越界时报出明确失败项而不再拿钳位值蒙混；覆盖成立才计算两条派生检查。
- **测试**：`test_gap换算共享实现禁止外插`（表内同值 + 表外 raises）；mock 链路全绿证明覆盖判定不误伤。

### P2-6 纯 mock 证据可被 real 加载 —— 已修复

- **修改文件**：`configs/motion_params.py`（`_check_report_binding`）、`scripts/calibrate.py`（motion 阶段检查 + promote 提示措辞）、`tests/test_calibration.py`
- **修改方式**：real 加载新增证据等级关卡——报告里**每个阶段**必须至少有一项 `evidence_type == "physical"` 的检查，否则 `ParamsError`（报告本身已被 report_sha256 + parameter_sha256 双向绑定）。为此 motion 阶段补了一项随样本证据分级变化的检查（从记录 command_deg 序列按轨迹+方向分组差分算实测最大单步角，须 ≤ `max_command_step_deg`；此前该阶段全是 offline 结构检查）。promote 的提示措辞改为"real 加载会直接拒绝"。
- **测试**：`test_pure_mock证据的配置禁止real加载`——完整八阶段 mock 链路 promote 出的 verified.json 在 real 模式被拒（修复前可加载）。主 pipeline fixture 改为把验证样本 metadata 标为 hardware（代表"已完成的真机验收"），继续覆盖哈希绑定正向链路。

### P2-7 起点偏差重规划路径零覆盖 —— 已修复（补测试，实现确认正确）

- **测试**（`tests/test_grasp_flow.py`）：`test_段起点偏差时重新规划到同一目标`（把实测起点掰偏 > tol，断言实际回放的段以实测起点重建、终点不变）；`test_笛卡尔段起点偏差必须报错不改斜线`（rebuildable=False → `PlanViolation`、零提交）。

### P3 各项

| # | 状态 | 说明 |
|---|---|---|
| P3-1 | 已修复 | 删除 `executor.wait_gripper_settled`（无调用方，`_step_gripper_open` 自带判据与超时）与 `motion_params._json_ready`（从未接线，且其"int/float 归一"意图与其自身实现不符）；顺带删除随之失去调用方的顶层导入与 `dataclasses` 多余 import。 |
| P3-2 | 已修复 | `_PrePlan.q_grasp` 字段删除；抓取位姿 IK 预检以裸调用保留并加注释。 |
| P3-3 | 已修复 | `_sim_meta("run" and "grasp", …)` → `_sim_meta("grasp", …)`。 |
| P3-4 | 已修复 | `g_lo` 死代码随 P2-2 重写删除。 |
| P3-5 | 已修复 | 拆为两条：`6 通道同型号`（offline）+ `motor.models/firmware 与采集 metadata 逐项一致`（随证据分级）；不再名不副实。 |
| P3-6 | 已修复 | 电流比例逐通道中位数；无参考数据的通道保留基值/ null，不再 `median*6` 冒充逐通道测量。 |
| P3-7 | 已修复 | `_fit_tool` 的 FK 改用被标定包自己的 `model.urdf_path`（经 `RobotKinematics` 直构，不再借仓库 SIM 配置的 MotionParams）。 |
| P3-8 | 已修复 | 加载期接触区间越出 gap_table 定义域时明确 `ParamsError`，不再 np.interp 静默钳位。 |
| P3-9 | 已修复 | 冗余表达式简化为 `st.monotonic() >= limit`。 |
| P3-10 | **未修复** | 会话级 `params` 的 `object.__setattr__`+finally 模式是既有测试的公共做法，全面改造为"每用例克隆配置"fixture 属于测试基础设施重构，超出最小修复范围；新增测试沿用了同一模式（finally 恢复）。建议后续统一。 |
| P3-11 | 已修复 | `check_between` 新增 `object_obb_fn`；`validate_segment` 为每个子步姿态重算持物盒（每子步多一次 FK）。冻结节点盒仅保留给"目标在空间静止"的调用方。 |
| P3-12 | 无需修复（记录） | `main.py` 为空且不在文档文件结构中，但 `readme.md` 把它写成程序入口（`python main.py`）；删除文件需同步改 readme，属文档整理，未擅自动。`vision.py` 按报告口径可接受。 |

## 二、构建结果

- 全部修改文件 `py_compile` 通过（configs/motion_params.py、scripts/calibrate.py、qingyun/grabbing/{safety,arm_control,executor,motor_control}.py、三个测试文件）。
- 环境：Qingyun conda env（Python 3.12，placo 0.9.16）。

## 三、测试结果

- **全量：269 passed（740.35s），0 failed，0 skipped**（原 254 项 + 本次新增/重写 15 项）。
- 每个 P1/P2 修复都配了"修复前失败、修复后通过"的复现用例（P1-1 薄墙与桌面净距、P1-2 基座系端点断言、P1-3 lower_arm×目标接触阶段复现、P1-4 放置不可达 CHECK 拒绝、P1-5 draft 加载 + 假总线 motor/joints 采集、P2-2 单位、P2-5 外插、P2-6 mock 配置 real 拒绝、P2-7 偏差重规划两分支、P2-4 锁存语义）。
- 无删除、无弱化任何原有断言；`test_持物包络挡住去路时被捕获` 的接触段断言按新语义**加强**（原来允许"整段跳过"，现在要求仍然判罚臂连杆）。

## 四、尚未解决的问题

1. **实机验收项**（P1 修复后的真实标定/实物测试）无法在本环境执行，属实机阶段工作。
2. P3-10（会话级配置改写的测试污染风险）未做结构性重构。
3. `main.py` 空文件与 readme 入口描述不一致，留给文档整理。

## 五、新发现的问题（修复过程中产生/处理，需 Reviewer 确认）

1. **修改了 `calibration/SIM_ROBOT/simulation/profile.json` 的 `gripper.contact_gap_range_m`：`[0.012, 0.032] → [0.020, 0.032]`**。原因：P1-4 的整路线校验立即暴露出 12mm 接触下界与仿真包其余几何/角度表不自洽——`moving_pad` 在该开度的抓取/提起位形低于桌面净距预算 3.7mm（实测），即该"标定接触区间"按文档 7.1 本就不该通过验证。指南 10.2/7.1 规定的处置是缩小经过验证的适用范围，因此改的是数据而不是弱化检查。请复核该取值与 mock 数据集自洽性。
2. `_check_hardware_prerequisites` 原来对非 motor 阶段要求 `timing.*` 非 null——timing 组第 5 阶段才拟合，会让 tool/workcell 实机采集第二次死锁（Reviewer 未提及，修 P1-5 时发现，已一并处理）。
3. 为让 P2-6 的"每阶段必须有 physical 证据"成立，motion 阶段新增了一项基于记录数据的检查（实测最大单步角 ≤ 命令步长，按 (trajectory_id, direction) 分组差分）。运行期日志导出工具必须保证 motion 记录含 `command_deg` 序列与 `direction`，否则该阶段将因"无任何 physical 检查"被 real 加载拒绝。
4. `test_calibration` 的 module 级 pipeline 现在把**验证样本** metadata.source 改写为 "hardware" 以代表已完成的真机验收（拟合样本仍是模拟源）；纯 mock 链路被 P2-6 新检查拒绝，由专项测试固定。

## 六、建议 Reviewer 重点复查的区域

1. **P1-3 的豁免范围决策**：`GRIPPER_CONTACT_LINKS`（夹爪子树整体 vs 仅指垫）——文档原文"仅指垫—目标"，实现因指座/活动指与指垫同组件、目标膨胀包络必然吞指座而放宽到子树；请核对 `tool_head` 胶囊建模与这条取舍是否可接受，或指座胶囊是否应重画。
2. **P1-4 的区间端点校验强度**：文档要求预规划"包括接触开度区间内的包络"，实现取两端点离散化；区间内非线性中点是否可能漏检，请评估是否接受（持物关系随 g 近似单调）。
3. **`contact_gap_range_m` 数据改动**（上节第 1 条）与 `load_calibration_profile` 的字段边界（哪些字段必须已测）。
4. **P2-6 关卡的强度**："每阶段 ≥1 physical 检查"是否足够，是否要求 physical 检查数量下限或逐关键检查 physical。
5. `CalibrationReader` 的 read-only 承诺（无任何写帧）与 q_bus/q_urdf 的换算出处。
6. P3-11 后 `check_between` 每子步重算 OBB 的耗时影响（全量套件从 419s → 740s，主要来自 P1-4 双端点校验 + 此条）。

---

**本报告不宣布项目通过最终审查；是否交付请由独立 Reviewer 复核后判断。**
