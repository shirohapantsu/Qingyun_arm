# upper 开发前复审后修正

> 日期：2026-09-22。依据：[初审报告](upper开发前方案初审.md) 第七章用户处理决定 + [复审报告](upper开发前方案复审_20260922.md) R01–R10（用户指示采纳并同步）。
> 本文记录本轮做了什么、改成了什么、留下什么。仅文档修订，**未修改任何运行代码、测试或标定数据**。

## 一、决策同步（实施文档 README）

| 决策 | 内容 | 状态 |
|---|---|---|
| D13 修订 | 原纳入项"main CLI（--mode mock/real --config）"作废 | 已写入 README 决策表 |
| D14 新增 | ignore 封顶（失败后 `ignore ≥ 当次 valid_count`）→ terminate("NO_GRASPABLE_TARGET")：不恢复、不人工确认、臂冻结；正常完成出口仅 NoTarget×3；NoTarget(skip_exhausted) 不属本封顶 | 决策表 + 问题原文已登记；D02 语境同步标注失效 |
| D15 新增 | 取消 main CLI；生产固定读 `configs/app.local.json`；独立 mock 入口 `scripts/run_mock.py`（固定读 `configs/app.mock.json`，豁免"生产禁止导入 tests"，注入统一 SimTime，跳过真实 asr/LLM init，复用生产会话函数，仿真 K/places 名义值注入） | 决策表 + 问题原文已登记 |
| D16 新增 | 配置模板去 example：`configs/app.json`/`secrets.json`/`vision.json`；实际为 `.local.json`；相对路径示例全部改为可真实解析形式 | 决策表 + 问题原文已登记 |
| D10 修正 | 经代码核实运动碰撞模型按固定 envelope 建模、视觉不输出高度——"运动兜底"表述降级为样本准入（高度 ≤ envelope[2] 量具抽检） | 已写入 D10 决定原文（R06） |
| R07 澄清项修订 | 原"直接使用现有 initialize"改为：须把已验证的启动握手（旧 Goal_Position 核对、部分使能清理、读回中断处置）整理为驱动公开生命周期接口，归 P1 | 已写入"后续澄清主题" |
| 头注 | 从"全部冻结可派发"改为注明各阶段真实就绪状态（R01/复审 §5） | 已修订 |

## 二、P0–P5 修订清单

### P0（公共契约与迁移）
- 文档同步清单具体化（5 项：上层文档 D08 标注/待办勾销、视觉契约 grade 错句与 §五 D14、落地方案 §6.3/§7.3/§7.4 标注、readme 三处、标定指南 A.2）。
- 关联决策补 D14/D16。

### P1（任务编排与生命周期）——修订量最大
- **R01/L1**：预检整体前移至 motor 构造前（§4 顺序重排：runlog → cfg → params → preflight → motor），"零串口写入"承诺成立；main 不二次调 `initialize()`。
- **R03**：冷启动补 `cloud_model.init(cfg)` 与 `LLM_INIT_FAILED`；三 init 全成才 ready（P1-T25）。
- **D14**：§6.2 exhausted 分支改为 terminate("NO_GRASPABLE_TARGET")（含精确触发条件与 skip_exhausted 区分）；§6.5 矩阵标注不再服务封顶；§6.6 完成原因仅 NO_TARGET_STREAK；P1-T06/T19 改写、新增 P1-T26。
- **D15**：无 CLI；新增 `scripts/run_mock.py` 文件责任；main 暴露可复用会话函数 `run_session`。
- **D16**：配置模板改名 `configs/app.json`/`secrets.json`；示例路径全部修正为相对 `configs/` 可解析形式。
- **初审 L7**：预检 vision 检查收窄为"存在且合法 JSON"，引用检查移交 P3 init。
- **初审 L5（经复审更正依据）**：预检新增 `yaw_offset ∈ {0,90}` 产品限制断言。
- **初审 L9**：主循环不再重复打印"任务完成"。
- **R07**：冷启动步骤 5 写入驱动启动握手要求；§8 实施步骤新增第 7 项（假总线验证）。
- 测试表更新至 T01–T26；日志 startup 行去 mode 改"入口"。

### P2（语音与云模型）
- D15 mock 说明：mock 入口不调 asr/cloud_model init。
- **R05**：ASR/LLM 端到端 deadline 要求（分项超时不等于墙钟上限，须可中止同步请求）；LLM 两次语义请求各自独立计时；新增 P2-T17 缓慢滴漏服务替身用例。

### P3（视觉检测与目标选择）
- **R02 几何管线重写**（§6 第 3 步）：RGB 先去畸变；严格区分 `Z_rgb`/`z_base`/`h`；z_top 逐像素 deproject→base 取稳健中值；足迹由 OBB 四角射线与桌面平面求交得 base XY 四边形，长宽/yaw 从中量取（禁止"像素×Z/f"和图像角直接转 base）；中心 XY 用射线-平面求交。新增 P3-T19（倾斜相机/外参平移/fx≠fy/边缘/畸变）与 P3-T20（去畸变对照）。
- **D16**：`vision.example.json`→`vision.json`；内部引用路径消除 configs/ 双重嵌套（`vision/...`）。
- **R06**：尺寸过滤边界说明三条（高度维运动不兜底、膨胀未含尺寸低估、逐轴充分条件与 yaw_offset 产品限制）。
- **R09/R10/初审 L6**：§8 验收口径——逐样本上限（不验均值）、模 180° yaw、分母含未检出样本、近圆果禁入本期样本集合（阈值 P4 冻结）、真值偏差控制。
- replay 帧与业务一致性要求（R04 引用 P5 §2）。

### P4（标定工具与参数发布）
- **R10**：§2.2 外参方法重写（平面标定板法/三维点法二选一、坐标系与标记偏移明确、求解器与点数匹配、输出取逆、合成测试覆盖取反错误）；§2.3 判据改逐样本上限 + 分母口径 + 真值偏差控制。
- **R08**：新增 §2.8 `scripts/record_pick_place_trials.py` 抓放验收入口（draft 上采集、导出 validate 兼容 JSONL、source=hardware、不绕生产门槛）；§3 流程插入第 5 步；P4-T08 新增。
- 贯穿约束：修改 yaw_offset 必须回审 P3（初审 L5）；样本准入冻结（R06/R09）。

### P5（集成测试与部署验收）
- **D15/R04**：mock 装配整体重写为独立入口 `scripts/run_mock.py` 契约（含 SimTime 注入代码块、仿真 K/places 名义值、replay 帧与脚本化结果一致、第二任务新帧序列）；验收命令更新。
- **D14**：E2E-02 改为封顶 terminate、E2E-12 改唯一完成出口审计；现场记录表完成原因去掉 IGNORE_EXHAUSTED。
- **R06/R09**：操作前提写入样本准入（高度 ≤45mm 抽检、近圆禁入）；**R07**：真机集成第 4 步加入启动握手三种注入验证。

## 三、源契约与其他文档同步

| 文件 | 修订 |
|---|---|
| 上层任务编排模块技术文档 | 头注加 D08/D14/D15；§1 状态机完成行；§2 run() 注释；§5 终止改唯一出口（废止标注）；§6 重试流程判定顺序按 D08/D14 重写；§11 接口改造待办勾销 |
| 视觉-plan-运动方案设想 | 头注加 D14；§一 删除"运动零引用 grade"错句（注明 P0 移除后成立）；§五 封顶改 terminate |
| 上层视觉运动完整功能落地方案 | 头注加 D08/D13/D14 说明；§6.3 尺寸行 D10 逐轴标注；§7.3 骨架 exhausted 分支废止标注；§7.4 恢复矩阵 D08/D13/D14 标注 |
| 真机参数测量与标定指南 | 附录 A.2 第 1 条修订为"已退回 draft"的事实状态（消除与同文 A.4 的矛盾） |
| 初审报告 | 第四章 L5 正文追加复审更正（充分条件非等价；断言保留但定性为产品限制）；§7.2 L5 行同步更正 |

## 四、本轮新引入的规格细节（供后续决策核对）

以下为实现闭环必须由本轮补写的具体值，非用户已确认事项，编码智能体可按此执行、用户可随时推翻：

1. 封顶 terminate 状态码字符串：`"NO_GRASPABLE_TARGET"`。
2. 独立 mock 入口路径 `scripts/run_mock.py`；mock 配置固定路径 `configs/app.mock.json`；生产配置固定路径 `configs/app.local.json`。
3. main 暴露的可复用会话函数命名 `run_session(cfg, arm)`。
4. LLM 计时口径：两次语义请求各自独立计时（复审 R05 要求明确，未指定方向，本轮取"各自独立"）。
5. 冷启动新终止码：`LLM_INIT_FAILED`。

## 五、遗留与边界

- **代码未动**：`arm_control._validate_target` 的 grade 校验、tests 旧构造、prompt.md 的 blueberry 段，均为 P0 的既定编码任务，本轮只做文档修订。
- **R09 的处理**：复审 R09 建议"近圆果禁入本期样本集合"（超出初审 L6 的"验收剔除"用户决定），用户指示按复审采纳，已写入 P3 §8 / P4 §2.3/§3 / P5 §4；圆度阈值数值仍由 P4 冻结（未预设）。
- **readme.md 未修订**：三处过时项（grade 接口表、watchdog、行数）已列入 P0 文档同步清单，随 P0 编码一并修订。
- 修订过程中发现编辑工具对同一文件的并行编辑会互相覆盖，已对全部修订文件做逐条 grep 复核，上文清单与文件实际内容一致。
