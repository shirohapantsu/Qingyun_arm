# P1 任务编排与运行生命周期技术文档

状态：已按 2026-09-22 初审/复审修正收敛（D14–D16、R01/R03/R04/R07 涉及项）。可作为编码规格派发；前置 P0 已合入。

范围（D06/D07）：本期只实例化草莓 Plan，使用草莓单套 motion profile。不实现 blueberry.py 或运动参数切换；蓝莓口令在 P2 按 invalid 处理。

## 1. 阶段目标与依赖

依赖 P0 的公共接口（六字段 VisionInterface + VisionThresholds）与运动回归基线。实现同线程同步的 main、plans、shutdown、runlog 和上层配置；借助不连接真实设备的替身打通任务循环。真实录音/云请求由 P2 实现，真实视觉由 P3 实现——本阶段为两者创建**契约桩**（异常类 + 函数签名，调用即抛对应硬错误），P2/P3 在原文件上替换实现，桩文件不得被重命名或移位。

已有运动控制负责单次抓取—放置及失败后的保持。P1 不在运动公开原语外拼装舵机指令，不另建线程或实时控制循环，不接 `should_stop` 回调（Ctrl-C 走 KeyboardInterrupt 现成路径）。

非目标：不实现录音/VAD/云调用（P2）、相机/检测/几何（P3）、落点与 K 的实测标定（P4）、端到端集成场景（P5）。

## 2. 文件责任

| 文件 | 责任 | 新增/修改 |
|---|---|---|
| `main.py` | 固定生产配置装配、冷启动、顶层任务循环（无 CLI，D15）；提供可被 mock 入口复用的会话函数 | 新增 |
| `scripts/run_mock.py` | 独立 mock 入口（D15）：固定读 `configs/app.mock.json`，组装 MockMotor/SimTime 与语音/云替身，复用生产会话函数与真实 Plan | 新增 |
| `configs/app_config.py` | AppConfig 等纯数据类、app.json 加载校验、secrets 加载、启动预检 | 新增 |
| `configs/app.json` | 非机密配置模板（字段说明与占位；实际运行为 `app.local.json`，D16） | 新增 |
| `configs/secrets.json` | 密钥空值模板（实际 `secrets.local.json` 进 .gitignore，D16） | 新增 |
| `qingyun/plans/__init__.py` | 固定注册表 `PLAN_REGISTRY = {"strawberry": StrawberryPlan}` | 新增 |
| `qingyun/plans/base.py` | 共用计数、分级流程、落点分配、结果分发、恢复与完成日志 | 新增 |
| `qingyun/plans/strawberry.py` | 草莓落点 ID 映射与面积阈值 K 占位（P4 回填） | 新增 |
| `qingyun/shutdown.py` | terminate、幂等退出 | 新增（删除 0 字节 `qingyun/watchdog.py`） |
| `qingyun/runlog.py` | JSONL 详细日志 + 终端简洁输出 | 新增 |
| `qingyun/asr.py` | 契约桩：`ASRHardError` + `init/listen_and_transcribe` | 桩（P2 替换） |
| `qingyun/cloud_model.py` | 契约桩：`LLMHardError` + `init/select_plan` | 桩（P2 替换） |
| `qingyun/grabbing/vision.py` | 契约桩：`NoTarget/VisionHardError` + `configure/init/get_target` | 桩（P3 替换） |
| `qingyun/grabbing/motor_control.py` | 启动握手整理为公开生命周期接口（R07，见 §4 步骤 5） | 修改 |
| `tests/mock_motor.py` | 补 `initialize()` 协议对等记录桩 | 修改 |
| `tests/support_upper.py` | FakeVision/FakeArm/FakeASR/FakeLLM、事件记录 | 新增 |
| `tests/test_plan_flow.py` / `test_main_flow.py` / `test_shutdown.py` | 业务分支、调用顺序、退出副作用 | 新增 |

生产模块禁止导入 tests；**唯一豁免是独立入口 `scripts/run_mock.py`**（D15：scripts 非生产运行链，可导入 tests/mock_motor.py 的 MockMotorController/SimTime）。业务替身通过测试内 monkeypatch 替换 `qingyun.plans.base` 命名空间里的模块引用。

## 3. 配置与公开签名

### 3.1 app.json（模板 `configs/app.json`；D16）

```json
{
  "profile_path": "../calibration/REAL_ARM/PC/profile.json",
  "vision_config_path": "vision.local.json",
  "prompt_path": "../prompt.md",
  "log_dir": "../logs",
  "asr": {"model": "qwen-audio-3.1-asr-flash", "cloud_total_timeout_s": 30.0},
  "llm": {"model": "<实际开通的 deepseek 模型名>", "base_url": "https://api.deepseek.com", "total_timeout_s": 30.0},
  "mock": {"asr_script_path": "../tests/fixtures/mock/asr_script.json",
            "vision_replay_dir": "../tests/fixtures/mock/frames"}
}
```

- 所有相对路径**相对该配置文件所在目录**解析，不依赖启动目录。上图是 `configs/` 下配置文件的合法示例（`../` 指向项目根）。
- **配置入口固定（D15，无 CLI）**：生产 `main.py` 固定读 `configs/app.local.json`；独立 mock 入口固定读 `configs/app.mock.json`。不存在 mode 参数与 mode 来源冲突问题。
- `mock` 段仅 `app.mock.json` 要求存在且文件有效；`app.local.json` 不得含 mock 段（未知键拒绝）。
- 超时数值为建议默认；目标机实测后由 P5 锁定，不在本阶段臆定"已验证"。

```python
# configs/app_config.py
@dataclass(frozen=True)
class AsrSettings:    # model: str; cloud_total_timeout_s: float (>0)
@dataclass(frozen=True)
class LlmSettings:    # model: str; base_url: str; total_timeout_s: float (>0)
@dataclass(frozen=True)
class MockSettings:   # asr_script_path: Path; vision_replay_dir: Path
@dataclass(frozen=True)
class AppConfig:
    profile_path: Path; vision_config_path: Path; prompt_path: Path; log_dir: Path
    asr: AsrSettings; llm: LlmSettings; mock: MockSettings | None

def load_app_config(path: Path) -> AppConfig
def load_secrets(path: Path) -> dict[str, str]     # {"dashscope_api_key","deepseek_api_key"}
def preflight(cfg: AppConfig, params: MotionParams,
              registry: Mapping[str, type["BasePlan"]]) -> None
```

`load_app_config` 校验：未知键拒绝、必填键齐全、超时为正数。`secrets.local.json` 固定路径 `configs/secrets.local.json`（P1 定义常量；加入 .gitignore，仓库只提交 `configs/secrets.json` 空值模板）。

### 3.2 启动预检（D13；**全部检查在 motor 构造之前完成**，失败 → terminate，此时未连串口、零舵机写入）

`preflight` 只依赖 `cfg` 与已加载的 `params`，不依赖 motor；§4 的顺序保证它在首次串口连接之前执行。

1. 注册表每个方案：`A_PLACE_IDS` 无重复；A/B/C 三组 ID 互不重叠。
2. 每个 place_id 存在于 `params.places`。
3. `AREA_THRESHOLD_M2` 已设置且 > 0（P4 未回填时为 None → 拒绝运行并提示"K 未标定"；mock 入口用注明"非实测"的名义值注入，见 P5 §2）。
4. `vision_config_path` 存在且为合法 JSON（L7 收窄：本阶段**不**解析其内部模型/标定引用，文件存在性检查整体移交 P3 init）。
5. `prompt_path` 存在且非空。
6. **yaw_offset 产品限制断言**：`params.grasp.yaw_offset_deg ∈ {0, 90}`，否则拒绝运行。这是本期已标定姿态范围的产品限制，不是几何学必然（逐轴包含对任意偏角已是充分条件，见复审 §4 对初审 L5 的更正）；P4 若重新标定 yaw_offset 必须回审 P3 尺寸过滤并同步本断言。
7. 生产入口附加：`secrets.local.json` 两个 API key 非空；profile status=verified（`load_motion_params(mode="real")` 已保证，此处不重复）。
8. mock 入口附加：`asr_script_path`、`vision_replay_dir` 存在；`log_dir` 可创建可写。

### 3.3 shutdown.py

```python
def terminate(status: str, detail: str) -> NoReturn:
    """唯一退出出口：打印 status/detail/阶段/现场处置指引 + sys.exit(1)。

    - 幂等：重复调用（含 atexit 期间）只记录一次，不再 sys.exit。
    - 不调用 hold_current/open_gripper/move_joints/configure_torque——任何出口
      不发新舵机指令、不回 home、不关扭矩（CAL-052 底线）。
    - atexit 钩子只做日志 flush 与无串口写入的资源关闭（如有）。
    """
```

### 3.4 runlog.py（D13）

```python
def init(log_dir: Path) -> str     # 建 logs/run-<UTC时间戳>-<pid>.jsonl，返回 session_id
def event(kind: str, **fields) -> None   # 追加一行 JSON（自动附 ts/session_id），立即 flush
def console(msg: str) -> None            # 终端简洁输出
```

单线程同步，无需锁；ndarray 字段先 `tolist()`。`scan_id`/`attempt_id` 由 plans 维护的单调计数器生成（每次 `get_target` / 每次 `grasp_and_place` 递增）。

### 3.5 plans

```python
# qingyun/plans/base.py
RETRYABLE_STATUSES = frozenset({GraspStatus.IK_FAILED, GraspStatus.PLAN_INVALID,
                                GraspStatus.GRASP_MISS, GraspStatus.GRASP_UNCERTAIN,
                                GraspStatus.GRIP_SLIP})
MAX_CONSECUTIVE_FAILURES = 10                      # D02

class BasePlan:
    TASK_ID: ClassVar[str]
    AREA_THRESHOLD_M2: ClassVar[float]             # D05；None=P4 未回填
    A_PLACE_IDS: ClassVar[tuple[str, ...]]         # 优品有序槽位（D04：分配即消耗）
    B_PLACE_ID: ClassVar[str]
    C_PLACE_ID: ClassVar[str]

    def __init__(self, arm: ArmController) -> None:
        # 计数器归零：no_target_streak/ignore/consecutive_failures/a_count/成功数/失败码计数
        # scan_id/attempt_id 计数器归零
    def run(self) -> None
    def classify(self, target: VisionInterface) -> str
    def next_place_id(self, grade: str) -> str     # 内部方法
```

```python
# qingyun/plans/strawberry.py
class StrawberryPlan(BasePlan):
    TASK_ID = "strawberry"
    AREA_THRESHOLD_M2 = None     # P4 标定后回填实测 m² 值；预检拒绝 None
    A_PLACE_IDS = ("strawberry_A_01", "strawberry_A_02", "strawberry_A_03")  # 数量 P4 落点标定后定
    B_PLACE_ID = "strawberry_B_bin"
    C_PLACE_ID = "strawberry_C_bin"
```

坐标不复制进 plan：`grasp_and_place` 按 place_id 从 `params.places` 读取；plan 只持有 ID 序列。落点命名与坐标标定归 P4；P1 测试在 simulation profile **副本**中追加这些 place_id（沿用 `tests/support.py` 的 write_profile 模式，不改仓库真配置）。

### 3.6 模块桩（P2/P3 替换前的契约面）

```python
# qingyun/asr.py（桩）
class ASRHardError(RuntimeError): ...
def init(cfg: AppConfig) -> None                    # P2：加载 VAD/设备/云配置
def listen_and_transcribe() -> str                  # 桩体抛 ASRHardError("ASR 未实现（P2）")

# qingyun/cloud_model.py（桩）
class LLMHardError(RuntimeError): ...
def init(cfg: AppConfig) -> None                    # P2：加载 prompt/key/endpoint
def select_plan(text: str) -> str                   # 桩体抛 LLMHardError

# qingyun/grabbing/vision.py（桩）
class NoTarget(Exception): ...
class VisionHardError(Exception): ...
def configure(thresholds: VisionThresholds, config_path: Path) -> None   # 桩体抛 VisionHardError
def init() -> None
def get_target(ignore: int = 0) -> VisionInterface
```

plans/base.py 通过 `from qingyun.grabbing import vision` 引用并在测试中整体替换该名字；NoTarget/VisionHardError 始终从桩/真实现导入，替身必须抛真实异常类。

## 4. 冷启动段（main，顺序固定）

```
1. session_id = runlog.init(log_dir 默认 logs/)  # 配置加载失败时尚无 JSONL，仅终端输出
2. cfg = load_app_config(固定路径 configs/app.local.json)     # D15：无 CLI
3. params = load_motion_params(cfg.profile_path, mode="real") # real 要求 verified
4. preflight(cfg, params, PLAN_REGISTRY)   # §3.2 全部检查；失败 → terminate
   —— 必须在步骤 5 之前：此时未连串口、零舵机写入（初审 L1）
5. motor = Sts3215MotorController(params)
   —— 构造即连接+initialize(verify_identity=True, enable_torque=True)；
      main 不再二次调用 initialize（初审 L1）
   —— R07 驱动启动握手要求（本阶段落实为 motor_control 公开生命周期接口）：
      使能前核对旧 Goal_Position 与当前反馈；部分使能失败逐轴失能清理；
      读回中断的状态与处置明确。复用 scripts/validate_home_waypoint_motion.py
      已验证的目标预置/力矩读回/使能前后位移核对步骤（含 CAL-052 处理）；
      用假总线检验顺序后再独立验证真机行为；main 不访问驱动私有状态
   —— 构造/初始化异常 → terminate("MOTOR_INIT_FAILED")
6. arm = ArmController(motor, params)              # should_stop 不接
7. 冷启动复位（上层文档 §8）：
     fb = motor.get_feedback()
     within = np.all(np.abs(fb.angles_deg - params.workspace.home_joints_deg)
                     <= params.motion.settle_position_tol_deg)
     if not within:
         arm.open_gripper()
         for wp in params.workspace.safe_waypoints_deg:  # 列表顺序，冻结
             arm.move_joints(wp)
         arm.move_joints(params.workspace.home_joints_deg)
     ——任一原语异常/wait 失败 → terminate("COLD_START_RECOVERY_FAILED")
   home 容差内不搬臂、不开爪（开机已空爪、场地已清是操作前提，不自动判空）
8. asr.init(cfg) → 异常 terminate("ASR_INIT_FAILED")
9. cloud_model.init(cfg) → 异常 terminate("LLM_INIT_FAILED")   # R03：原规格遗漏
10. vision.configure(VisionThresholds(...从 params 提取，P0 契约), cfg.vision_config_path)
    vision.init() → VisionHardError → terminate("VISION_INIT_FAILED")
    （init 阶段故障不得伪装成 NoTarget）
11. runlog.event("ready"); console("系统就绪，等待语音指令")
    —— asr/cloud_model/vision 三者全部初始化成功后才允许 ready（R03）
```

main 同时暴露可被独立 mock 入口复用的会话函数（如 `run_session(cfg, arm) -> NoReturn`，含冷启动复位与顶层循环），mock 入口不复制业务逻辑（D15/R04）。

## 5. 顶层主循环与异常映射

```python
while True:
    text = asr.listen_and_transcribe()          # ASRHardError → terminate("ASR_HARD_ERROR")
    if not text.strip():
        continue
    task = cloud_model.select_plan(text)        # LLMHardError → terminate("LLM_HARD_ERROR")
    if task == "invalid":
        continue
    plan = PLAN_REGISTRY[task](arm)             # 每任务新实例，计数器归零
    plan.run()                                  # 正常返回 = 任务完成（仅 NoTarget×3，D14）
    # 不在此重复打印"任务完成"：统一由 _finish 输出（初审 L9）
```

| 异常来源 | 处置 |
|---|---|
| ASRHardError / LLMHardError | terminate（臂在 home） |
| VisionHardError（含 init/get_target） | terminate（臂在 home 或恢复后位） |
| PLAN 内 KeyboardInterrupt | WAITING/ASR 阶段 → terminate；抓取中由 arm_control 转 ABORTED → 分发表 → terminate；恢复 input() 中 → terminate |
| `SystemExit` | terminate 自己发的 exit(1) 直通，不包装 |
| 其余未预期异常 | terminate("UNEXPECTED", repr) |

PLAN 运行期间不录音、不提交云请求、不缓存下一任务。任务完成不退出程序，回 WAITING。生产入口无"跑完即退"分支。

## 6. plans 循环：计数器、分发顺序与恢复矩阵

### 6.1 计数器（D02/D03/D04）

| 事件 | no_target_streak | ignore | consecutive_failures | a_count |
|---|---:|---:|---:|---:|
| 新建 Plan | 0 | 0 | 0 | 0 |
| 视觉成功返回 | 0 | 不变 | 不变 | 分级为 A 并分配时 +1 |
| NoTarget | +1 | 0 | 不变 | 不变 |
| SUCCESS | 0 | 0 | 0 | 不变（不回滚） |
| 重试类失败 | 不变 | +1 | +1 | 不变（不回滚） |

### 6.2 run() 分发顺序（固定，不可重排）

```python
while True:
    try:
        target = vision.get_target(self.ignore)
    except NoTarget:
        self.ignore = 0; self.no_target_streak += 1
        if self.no_target_streak >= 3:
            self._finish("NO_TARGET_STREAK"); return
        continue
    except VisionHardError:
        terminate("VISION_HARD_ERROR", "视觉管线故障")
    self.no_target_streak = 0
    self._validate_target_contract(target)        # §6.4

    grade = self.classify(target)
    place_id = self.next_place_id(grade)          # A 越界 → terminate（§6.3）
    self.attempt_id += 1
    runlog.event("assign", grade=grade, place_id=place_id, a_count=self.a_count,
                 valid_count=target.valid_count, ignore_used=self.ignore, ...)

    result = self.arm.grasp_and_place(target, place_id)

    if result.stage == "FAULT_UNCONTROLLED":      # 1. 失控阶段最先
        terminate("FAULT_UNCONTROLLED", result.reason)
    if result.status == GraspStatus.SUCCESS:      # 2. 成功
        self.ignore = 0; self.consecutive_failures = 0; self.success_count += 1
        continue
    if result.status not in RETRYABLE_STATUSES:   # 3. 直死类
        terminate(result.status.value, result.reason)

    # 4. 重试类
    self.ignore += 1; self.consecutive_failures += 1
    exhausted = self.ignore >= target.valid_count
    if exhausted:                                 # 5. D14：判定无可抓取目标
        terminate("NO_GRASPABLE_TARGET",
                  f"ignore={self.ignore} ≥ valid_count={target.valid_count}，"
                  f"末次 {result.status.value}/{result.stage}")
        # 不恢复、不人工确认、不回 home、臂冻结于当次失败位（D14）
    if self.consecutive_failures >= MAX_CONSECUTIVE_FAILURES:   # 6. 预算
        terminate("FAILURE_BUDGET_EXHAUSTED", ...)              #    不恢复、臂冻结
    self._recover(result)                         # 7. 恢复后下一轮视觉
```

- 每次 NoTarget 之间不加 sleep；三连调每次都是真实新扫描。
- 连续三次 NoTarget 正常返回（**唯一正常完成出口**，D14）；每次视觉成功必须 `valid_count > requested_ignore`，不满足走 §6.4 契约违约。
- D14 精确触发条件：**本次重试类失败使 ignore 递增后，`ignore ≥ 本次返回该目标的 valid_count`**。视觉另因新扫描候选数变化抛出的 `NoTarget(skip_exhausted)` 不属于本封顶，仍走 NoTarget 分支（ignore=0、no_target_streak+1）；该分支不证明每个物理目标都已尝试。
- 封顶 terminate 不发任何新运动命令、不 reset、不 input()；"臂冻结"是沿用既有保持状态的业务描述，不代表软件在失控情况下仍能保证物理静止。

### 6.3 next_place_id（D04）

```python
def next_place_id(self, grade: str) -> str:
    if grade == "A":
        if self.a_count >= len(self.A_PLACE_IDS):
            terminate("A_SLOTS_EXHAUSTED", "优品槽位耗尽（GRASP_MISS 空耗捅穿摆桌保证）")
        pid = self.A_PLACE_IDS[self.a_count]; self.a_count += 1
        return pid
    if grade == "B": return self.B_PLACE_ID
    if grade == "C": return self.C_PLACE_ID
    terminate("PLAN_CONFIG_INVALID", f"未知 grade {grade!r}")
```

不回滚、不降级（A 耗尽不降 B、不循环覆盖 A_01）；任何运动结果（含运动前拒绝）都不回调 a_count。

### 6.4 视觉返回值契约复核（plans 侧）

`_validate_target_contract(target)`：`length_m >= width_m > 0` 且为有限 float；`ripe` 严格 bool；`valid_count` 正整数且 `> self.ignore`（当前请求值）；`yaw_deg ∈ [-90,90)`。违约 → terminate("VISION_CONTRACT_VIOLATION")，不触发抓取、不计 NoTarget。position/yaw 的深层校验归运动（INVALID_INPUT 直死），不重复实现。

### 6.5 恢复矩阵（D08 后定稿；D14 后仅服务"继续下一轮"路径）

> 注意：ignore 封顶（D14）在 §6.2 第 5 步直接 terminate，**不再进入本矩阵**。

| 返回情形 | 处置 |
|---|---|
| IK_FAILED / PLAN_INVALID，`recovery_required=False` | 运动前失败（臂未动、在 home、空爪）：不 reset、不搬臂，直接进入下一轮视觉（ignore 已 +1） |
| IK_FAILED / PLAN_INVALID，`recovery_required=True` | **terminate**（D08：一律终止，不区分 holding，臂冻结于失败位） |
| GRASP_MISS，`recovery_required=True` 且 `holding=EMPTY` | 空爪确定：自动 `reset_fault()` → 依次 `move_joints(safe_waypoints)` → `move_joints(home)` → 下一轮视觉 |
| GRASP_UNCERTAIN / GRIP_SLIP，`recovery_required=True` | 打印现场状态（status/stage/holding/reason）→ `input("确认物体已处置、可继续后回车...")` 阻塞等待；**回车即确认**（D13：不要求特定文本，空回车同样算确认）；EOFError/KeyboardInterrupt → terminate("MANUAL_CONFIRM_ABORTED")。确认后 `reset_fault()` → 中转 → home → 下一轮视觉 |
| GRASP_MISS/GRASP_UNCERTAIN/GRIP_SLIP 带 `recovery_required=False`，或 MISS 报 `holding≠EMPTY` 等状态与标志组合违约 | terminate("RESULT_CONTRACT_VIOLATION")，保留原 result 供定位 |
| reset_fault / 任一回程原语异常 / move 未到位 | 立即 terminate("RECOVERY_FAILED")，不追加 ignore 后再试恢复 |

回程路线冻结为 profile `safe_waypoints_deg` 列表顺序 + home，与冷启动复位一致；正常 SUCCESS 已由运动 RETURN 保证 home，不额外回零。确认期间不后台请求视觉或发送指令（同步阻塞天然保证）。

### 6.6 _finish（任务完成出口）

`runlog.event("task_done", reason=..., success=..., failure_counts=..., a_slots_used=...)` + 终端一行：`任务完成：结束原因=NO_TARGET_STREAK|IGNORE_EXHAUSTED，成功=N，失败码计数=...，已分配优品槽位=M`。终端必须含"任务完成"字样（契约要求），同时不掩盖未抓走目标。

## 7. 日志事件（D13，JSONL 最低字段）

| 事件 | 字段（除 session_id/ts 外） |
|---|---|
| startup | 入口（生产/mock）、配置文件路径、profile/vision_config/prompt 路径与哈希 |
| preflight_failed / ready | 失败项 / 就绪 |
| asr_result / llm_result | 转写文本、task、请求耗时（脱敏） |
| vision_scan（P3 产生，此处定字段名） | scan_id、ignore、valid_count、候选数、逐项拒绝原因 |
| assign | grade、place_id、a_count、valid_count、ignore_used、attempt_id |
| grasp_result | status、stage、reason、holding、recovery_required、attempt_id、耗时 |
| recovery | 方式（auto/manual/skip）、reset/各回程段结果 |
| task_done / terminate | §6.6 字段 / status、detail、所处阶段 |

grade 只出现在 plans 自己的日志；运动侧只见 place_id。启动、抓取返回、恢复、任务结束为必记；视觉扫描行由 P3 按同 session 写入或经 runlog 写入（P3 定）。

## 8. 实施步骤

1. `configs/app_config.py` + `configs/app.json` / `configs/secrets.json` 两个模板 + .gitignore 追加 `configs/secrets.local.json`、`configs/app.local.json`、`configs/vision.local.json`；`load_app_config/load_secrets/preflight` + 参数化负向测试（沿用 tests/support.py 的临时目录改写模式）。
2. `qingyun/shutdown.py` + `qingyun/runlog.py` + test_shutdown（幂等、退出码 1、无舵机调用、atexit 不二次退出）。
3. 三个模块桩（asr/cloud_model/vision）+ 删除 `qingyun/watchdog.py`；tests/mock_motor.py 补 `initialize()` 记录桩。
4. `plans/base.py` + `strawberry.py` + `__init__.py`；tests/support_upper.py（FakeVision 按脚本返回目标/异常、FakeArm 按脚本返回 GraspResult 并记录事件序列、FakeASR/FakeLLM）。
5. test_plan_flow：§9 场景全表。
6. `main.py`：固定配置装配、冷启动（含 `cloud_model.init`，R03）、主循环、异常映射、可复用会话函数；`scripts/run_mock.py` 独立入口（D15，豁免 tests 导入，注入 SimTime 与替身）。test_main_flow：冷启动分支、预检前移（零串口写入断言）、退出路径。
7. 驱动启动握手公开接口（R07）：整理自 `scripts/validate_home_waypoint_motion.py` 已验证步骤；假总线测试旧目标不一致、部分使能失败、读回中断的处置顺序。
8. 全量回归 `python -m pytest tests -q`；现有运动测试不得回归。

## 9. 测试场景（必测）

| 编号 | 场景 | 预期 |
|---|---|---|
| P1-T01 | invalid 或空转写 | 不创建 Plan、不调用视觉/抓取 |
| P1-T02 | 连续两个有效任务 | 两个 Plan 实例；计数器各自从零 |
| P1-T03 | 连续 NoTarget×3 | 三次新调用后完成；调用间无 sleep |
| P1-T04 | NoTarget→目标→NoTarget | 中间目标打断 NoTarget 连续计数 |
| P1-T05 | V=12，连续 10 次可恢复失败 | 第 10 次：ignore=10<12 未封顶，预算 terminate；无第 10 次恢复 |
| P1-T06 | V=10，第 10 次失败 | 同次封顶先判（D14）：terminate("NO_GRASPABLE_TARGET")；无恢复、无 input()、零新运动命令 |
| P1-T07 | 若干失败后 SUCCESS | ignore 与失败计数归零；失败目标可再次入选 |
| P1-T08 | 失败后 NoTarget 再获目标 | ignore 清零、连续失败数不清零 |
| P1-T09 | A 第一次 GRASP_MISS，下一个 A 成功 | 用 A_01、A_02，不回收 A_01 |
| P1-T10 | A 名额耗尽仍有目标 | 下一次运动调用前 terminate |
| P1-T11 | 可重试 status 但 stage=FAULT_UNCONTROLLED | 优先 terminate；零追加串口调用 |
| P1-T12 | 恢复中某原语抛异常 | 不进视觉、不打印任务完成，统一退出 |
| P1-T13 | IK_FAILED，recovery_required=True（D08） | 立即 terminate；无 reset、无人工确认、臂冻结 |
| P1-T14 | IK_FAILED，recovery_required=False | 无 reset/搬臂调用；ignore+1 直接下次视觉 |
| P1-T15 | GRASP_UNCERTAIN 后 input() 回车 | reset→中转→home→继续跳选 |
| P1-T16 | input() 抛 EOFError / Ctrl-C | terminate("MANUAL_CONFIRM_ABORTED") |
| P1-T17 | GRASP_MISS 但 holding=HOLDING | RESULT_CONTRACT_VIOLATION terminate |
| P1-T18 | 预检各失败分支（缺 place_id / K=None / A 重复 / key 空 / mock 文件缺） | terminate 且零串口写入 |
| P1-T19 | 两种任务完成出口 | 终端含"任务完成"+原因+成功数+失败码计数+已分配槽位 |
| P1-T20 | 冷启动 home 容差内 / 容差外 | 容差内零搬臂；容差外按 open→waypoints→home 顺序 |
| P1-T21 | 冷启动复位原语异常 | terminate("COLD_START_RECOVERY_FAILED") |
| P1-T22 | --mode mock 路由 | 使用 simulation profile 与替身，不构造真串口对象 |
| P1-T23 | get_target 抛 VisionHardError | terminate；不混入 NoTarget 计数 |
| P1-T24 | 视觉契约违约（valid_count≤ignore、负尺寸、非 bool ripe） | terminate("VISION_CONTRACT_VIOLATION")，零抓取调用 |

FakeArm 验证调用顺序与命令数量；现有 ArmController+MockMotor 的真实返回路径集成不在本阶段重复建设（P5 覆盖）。

验收命令：

```bash
python -m pytest tests/test_plan_flow.py tests/test_main_flow.py tests/test_shutdown.py -q
python -m pytest tests -q
```

## 10. 交接给后续阶段

- P2：替换 asr/cloud_model 桩（保留异常类名与签名）；`init(cfg)` 签名以本文 §3.6 为准；mock 入口**不调用**这两个 init（D15），其业务函数由 P5 的脚本化替身接管。
- P3：替换 vision 桩；`configure(thresholds, config_path)` 的 VisionThresholds 按 P0 契约；vision 配置文件为 `configs/vision.json`（模板）/`vision.local.json`（实际，D16），其 JSON schema 由 P3 定义并同步进 `configs/app.json` 注释。
- P5：独立 mock 入口 `scripts/run_mock.py` 的装配契约（SimTime 注入、替身替换边界、仿真 K 注入）按本文 §4/§8 与 P5 §2 实现；日志事件字段以本文 §7 为验收基准。
- 交付物：可离线跑通 T01–T26 的仓库 + JSONL 样例日志。

## 11. 关联决策

D01（英文 task）、D02（N=10）、D03（临时跳选）、D04（槽位即消耗）、D05（独立 K，本期草莓）、D06/D07（仅草莓、蓝莓→invalid）、D08（运动后规划失败一律 terminate）、D13（init 入口/JSONL/预检/回车即确认；main CLI 项已被 D15 作废）、D14（ignore 封顶→terminate）、D15（无 CLI、独立 mock 入口）、D16（配置模板命名）。初审 L1（预检前移）/L7（预检收窄）/L9（取消重复打印），复审 R03（cloud_model.init）/R04（统一模拟时钟）/R07（驱动启动握手）。
